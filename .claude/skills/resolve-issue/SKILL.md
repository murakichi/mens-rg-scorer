---
name: resolve-issue
description: open な GitHub issue を1周につき1件ずつ順に片付ける。引数にラベル名（例 bug / enhancement / "bug,question"）を渡すとそのラベルだけを対象に古い順で消化し、issue番号を渡せばそれを直接扱う。引数なしなら既定の優先順。調査→修正→テスト→PR→セルフレビューまで通し、採点ロジックに触れない変更は自分でマージ、src/scoring/ を触った変更はレビュー所見を付けてオーナーに預ける。/loop /resolve-issue <ラベル> で回す想定。
---

# resolve-issue — issue を1件片付ける（1イテレーション）

**1周で扱うのは1件だけ。** 複数を並行して抱えない（PR が混ざると人がレビューできない）。

## 使い方

```
/resolve-issue bug              # bug ラベルを古い順に1件
/resolve-issue bug,question     # どちらかのラベルが付いたものを古い順に1件
/resolve-issue #23              # 番号指定でその issue を扱う
/resolve-issue                  # ラベル指定なし（既定の優先順）
/loop /resolve-issue bug        # 延々と消化させる
```

引数の解釈：

| 引数 | 対象 | 並び |
|---|---|---|
| ラベル名（カンマ/スペース区切り、複数可） | **いずれか**のラベルが付いた open issue | 作成が古い順 |
| `#<番号>` / `<番号>` | その issue | — |
| なし | すべての open issue | 既定の優先順（下） |

**既定の優先順**（このリポジトリの運用。引数があれば引数が優先）：
`[blocked]`（作業が止まっている） > `bug`（採点が間違っている） > `[docs]` > `[idea]`。
接頭辞は `report-issue` スキルで起票されたときに付く目印。

## 1周の流れ

### 1. 候補を集める

```
gh issue list --state open --label bug --limit 30
```
`gh` が無い環境（Claude Code on the web など）では GitHub MCP の
`mcp__github__list_issues`（`state: "OPEN"`, `labels: [...]`, `orderBy: "CREATED_AT"`, `direction: "ASC"`）。

**除外するもの**：

- 既に PR が出ている issue（`gh pr list --state open` / `mcp__github__list_pull_requests` で
  本文に `Closes #N` があるものを確認）
- 直近に「判断待ち」コメントを書いて、オーナーの返信が無いもの（同じコメントを二度書かない）
- `wontfix` / `duplicate` ラベルが付いているもの

### 2. 1件選ぶ

引数でラベルが指定されていれば、その集合の **古い順に1件**。指定が無ければ既定の優先順 →
同順位なら古い順。番号指定ならそれ。選んだ理由は最後の報告に1行で書く。

### 3. 分類する

issue を読み、関連コード・ルールを確認したうえで、次のどれかに決める：

- **自力で片付く** → 手順4へ
- **オーナーの判断が要る**（仕様の選択、UI の方針、優先度）→ 手順7へ
- **もう有効でない**（実装済み・再現しない・重複）→ 根拠を1〜3行コメントして
  `gh issue close --reason "not planned"`（または completed）。手順8の作業ログを書いて1周を終える。

判断に迷ったら「オーナーの判断が要る」側に倒す。**採点ロジックの仕様は
`mens-rg-rules.md` を根拠に出せるときだけ自分で決める。**

### 4. 直す

- 変更は **その issue の範囲だけ**。ついで修正を混ぜない（気づいたら `report-issue` で別 issue）。
- 採点ロジック（`src/scoring/`）を触ったら **必ずテストを追加/更新** する
  （`src/scoring/__tests__/`、期待値はルールから手計算）。
- リファクタなら **挙動が変わらないこと** をテストで示す（既存テストが緑のまま）。

```
npm test
npm run build
```
両方緑になるまで直す。テストが落ちたまま次へ進まない。

### 5. コミットして PR

- ブランチ：`claude/issue-<番号>-<短いslug>`（環境から作業ブランチを指定されている場合はそれに従う）
- コミットメッセージは既存の履歴に合わせて日本語1行（例：`生成：手具ごとの重みを constants.ts に集約した (#63)`）
- `git push -u origin <branch>` → PR を作成し、本文に **`Closes #<番号>`** を入れる
  （`gh pr create` / `mcp__github__create_pull_request`）
- PR 本文には「何を変えたか」「ルール上の根拠」「テストで何を確かめたか」を各1〜3行。
- **手順8の作業ログを同じ PR に含める**（コード変更と同じコミットでよい）。

### 6. レビューして、マージするか預けるか決める

**まずセルフレビュー**（PR を作ったら必ず実施）：

```
/code-review high
```
指摘が出たら直して push し、`npm test` / `npm run build` を取り直す。
**PR に CI は無い**（`.github/workflows/deploy.yml` は main への push でしか動かない）ので、
緑の根拠はローカルのこの2コマンドと、セルフレビューの結果しかない。

そのうえで、**変更したファイル** で2つに分かれる：

| 変更の中身 | 扱い |
|---|---|
| `*.md` / `work-logs/` / `test-reports/` / `.claude/` / `src/**/__tests__/` **だけ** | **自分でマージしてよい**（下の条件を全部満たすとき） |
| `src/` の実装に1ファイルでも触れた（特に `src/scoring/`） | **マージしない。** オーナーに預ける |

**自己マージの条件（すべて満たすこと）**：

1. 変更ファイルが上の「だけ」の範囲に収まっている（`git diff --name-only origin/main...HEAD` で確認）
2. `npm test` と `npm run build` が緑
3. `/code-review` の指摘がゼロ、または全部対応済みで再レビューが緑
4. その issue に「オーナーの判断が要る」論点が残っていない

満たすなら `gh pr merge --merge`（/ `mcp__github__merge_pull_request`）でマージし、
`git checkout main && git fetch origin main && git reset --hard origin/main` でローカルを同期する。
`Closes #N` により issue は自動でクローズされる。1つでも欠けたら **預ける側** に倒す。

**預ける場合**：セルフレビューの所見を PR に残し（`/code-review --comment`、指摘ゼロなら
「セルフレビュー済み・指摘なし」と1行コメント）、`npm test` / `npm run build` の結果を添えて
「レビュー済み・マージ待ち」で終える。**採点結果が変わりうる変更は必ず人が見る。**

### 7. オーナーの判断が要るとき

issue に **選択肢と推奨** をコメントして、その周は終わる。

```markdown
## 調べたこと
<コード・ルールの該当箇所。file:line と §x.y>

## 選択肢
- A: <案> — 影響: <…>
- B: <案> — 影響: <…>

## 推奨
<A か B か。理由1〜2行>

決まれば実装します（どちらでも作業量は同程度 / A のほうが小さい など）。
```
ラベルが付けられるなら `question` を付ける。**コードは触らない。**
この場合も手順8の作業ログは残す（調べたことを次の周に渡すため。ログだけをコミットしてよい）。

### 8. 作業ログを書く（毎周必須）

`work-logs/YYYY-MM-DD-<slug>.md` を `work-logs/_TEMPLATE.md` から1本作る。日付は環境の今日の日付。
必ず埋めるのは **対象issueと選んだ理由 / 調べたこと（ルール§・`file:line`）/ やったこと /
`npm test`・`npm run build` の結果 / セルフレビューの結果 / 結果（PR・マージ有無・issueの扱い）/
申し送り**。同じ日に2本目なら `-2` を付ける。コードを触らなかった周（判断待ち・クローズ）でも書く —
**調べて分かったことが次の周の入力になる。**

### 9. 報告（1行〜数行）

- 扱った issue 番号とタイトル（どの対象集合から選んだかも）
- 結果：**マージ済み** / レビュー済みでマージ待ち / コメントして判断待ち / クローズ
- セルフレビューの指摘件数と対応
- 残っている対象 issue の件数
- 書いた作業ログのパス

**候補が1件も無ければ**「対象 issue なし」とだけ報告して終わる（作業ログも不要）。
`/loop` を止めるかはオーナーが決める。

## やらないこと

- 1周で2件以上の issue を触る
- ルールの根拠なしに採点値・採点仕様を変える
- **`src/` の実装に触れた PR を自分でマージする**（セルフレビューが緑でも預ける）
- セルフレビューを飛ばしてマージする、指摘を「軽微」と自己判断して残したままマージする
- 大きなリファクタを一息でやる（分割して、残りは `report-issue` で起票）
- テストを skip / 削除して緑にする
- issue を根拠なくクローズする
- 作業ログを後からまとめて書く（1周ごとに1本、その周のうちに）
