# 作業ログ 2026-09-18: 連続を終える技の後に何か続く並びを警告にした

- 日付: 2026-09-18
- 実施者: 手動（オーナー報告「投げタンできりもみ転回を使うとその後に前転をしてるので修正して」）
- 対象issue: なし

## 調べたこと（再現しなかった）

現在の生成では**再現しない**。測った範囲：

| 測ったもの | きりもみ転回を含む | その後に徒手がある |
| --- | --- | --- |
| 候補（4手具×100シード） | 608件 | **0件** |
| 生成結果（4手具×40構成） | 61シリーズ | **0件** |
| 全パターン総当たり（strip前・60シード） | — | **0件** |

理由：`noRollAfter` が `endsChain`（とび前転・きりもみ・きりもみ転回・側宙・ダイビング）も見るように
したのが `3f84bf1`で、きりもみ転回を前方系の続きに足した `d4de8ee` はその後。
つまり**きりもみ転回が投げタンに出るビルドには必ずこの修正が入っている**。
`前転` を足す経路は2つ（連続途中の `rollAfterChance` と投げタンの締めの `rollFinish`）しかなく、
どちらも塞がっている。

生成される形はこうなっている：`投げ→前宙→きりもみ転回→キャッチ` /
`投げ→伸身前宙1回ひねり→きりもみ転回→キャッチ→投げ→キャッチ`。

## やったこと（再現しないが、起きたら見えるようにした）

手入力・インポート・古い保存データからは同じ並びが作れてしまい、いまは**画面に何も出ない**。
そこで検査を1本足した：

- `tumblingChainEndErrors(series)`：`CHAIN_END_SKILLS` の技の後に**技・徒手動作**が続いていたら
  「この技の後に技・徒手動作は続けられない」を返す（**キャッチは対象外**。
  投げ→前宙→きりもみ転回→キャッチ が本来の形）
- `tumblingFlowErrors` に含めた（自動生成の自己検算に乗る）
- `SeriesCard` が手具の流れの警告（`checkApparatusFlow`）と並べて表示する。採点には影響しない

とび前転は `CHAIN_END_SKILLS` に入っているが `TUMBLING_CONNECTS`（ロンダート・バク転・
ハンドスプリング）には入っていないので、つなぎ技として使う形と競合しない
（CLAUDE.md に「つなぎはロンダート/側転/ハンドスプリング/とび前転」と書いてあったのは実装と違う）。

## 申し送り

- **再現できていないので、オーナーが見た並びを教えてもらえれば追える。**
  古いタブ（ホットリロード前）・保存済みテンプレート・共有URLのどれかだと思われる。
  いまは入力画面に⚠が出るので、同じ並びが残っていれば見つけられる
- とび前転を手入力でつなぎ技に使うと今回の警告が出る。規則上はA難度のつなぎになり得るが、
  首から背中にかけて着地するので続けられない、という扱いで統一した

## テスト

`autoTumblings.test.ts` に2件（報告された並びが警告になる・キャッチは続けてよい・
`CHAIN_END_SKILLS` すべてで技を続けたら警告／候補は4手具×30シードで警告0件）。
`npm test` **591 passed (15 files)**、`npm run build` 成功。
