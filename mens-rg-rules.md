# 男子新体操 採点計算アプリ ルール仕様メモ

採点計算アプリの実装に対応するルール定義のドキュメント。
正式な難度値・加点点数が確定したら、本書とアプリ内の定義テーブル（`SKILL_LIST` / `BONUS_RULES` / `REQUIRED_CHECKS`）を同期させる。

---

## 1. モード

| モード | 人数 | 実装状況 | 備考 |
|--------|------|----------|------|
| 個人 | 1名 | 実装中 | 考慮要素が少ないため先行実装 |
| 団体 | 5名 | 実装中 | 5人ルール。シリーズ難度（3人到達・5人同時・交差・組運動）と団体D加点を実装済み。A/E実施減点は今後 |

## 1.5. 採点の3カテゴリ

新体操の採点は次の3カテゴリで構成される。

| カテゴリ | 内容 | 方式 | 計算 |
|----------|------|------|------|
| **D（難度）** | 技の難度・加点要素 | 加点方式 | 各加点の合計 |
| **A（芸術と多様性）** | 演技構成・必須要素の充足 | 10点満点から減点 | max(0, 10 − A減点合計) |
| **E（実施）** | 出来栄え | 10点満点から減点 | max(0, 10 − E減点合計) |

**合計 = D + A残点 + E残点**

- A・Eの残点は0で止まる（マイナスにならない）。
- D：タンブリング難度点、徒手難度点、連続投げ加点、技術加点、手具操作加点、二つ投げ4動作加点、E難度ボーナス。
- A：方向系不足減点、投げ回数不足減点、連続宙返り減点、つなぎ技手具操作なし減点、無手具操作減点。
- E：各シリーズに自由入力する実施減点の合計（出来栄え判定はユーザー入力）。

---

## 2. タンブリング構成（個人）

- 1演技につきタンブリングは **3本**。
- 各本は技を順につないで構成する（例：ロンダート → 後方宙返り一回半ひねり → ロンダート → ダイビング前宙）。
- 入力UIは技をプルダウンで横に連結し、各技に「種具操作」「投げ」のチェックを付与する。

### 投げタン
- タンブリングを実施しながら投げを行うものを「投げタン」と呼ぶ。
- アプリ上は技ごとの「投げ」チェックで表現する。

---

## 3. 個人タンブリングの必須要素

3本全体を通して、以下の要素をすべて満たす必要がある。

| キー | 必須要素 | 判定内容 |
|------|----------|----------|
| `threeDirections` | 方向の網羅 | 前方系・側方系・後方系を演技全体に**すべて**含む |
| `throwTumbling` | 投げタン | 3本のうち**1本以上**で投げを実施 |
| `tripleSalto` | 連続宙返り | 3本のうち**1本以上**で宙返りを**3回以上連続**で実施 |
| `connectSkill` | つなぎ技 | 3本のうち**1本以上**につなぎ技を含む（宙返り間にA難度を挟む。手具操作は成立要件でないが、無いと減点） |

### 連続宙返りのカウント仕様
- 宙返り系の技が**途切れず連続した回数**を数える。
- A難度技（側転・ロンダート・バク転・ハンドスプリング・とび前転・投げ）は宙返りに含まれず、**連鎖をリセットする**。
- 例：`ロンダート → 宙返り → 前宙` は最大2連続（ロンダートで切れる）→ 3連続不足。
- 例：`宙返り → ロンダート → 宙返り → 宙返り` も最大2連続。
- ※連続宙返りのカウントと、つなぎ技の判定（宙返り→A難度→宙返り を「つなぎ」と認める）は別要件として扱う。

### 系統の定義

| 系統 | 例 |
|------|-----|
| 前方系 | 前宙、ダイビング前宙 など |
| 側方系 | 側転、ロンダート、側宙 など |
| 後方系 | 後方宙返り 系 など |

### つなぎ技の定義
- 宙返りの連続の**間に A難度の技**（ロンダート、バク転 等）を挟むこと。
- つなぎ技として認められるのに手具操作は**不要**。
- ただし、つなぎで挟まる A難度の技で**手具操作を実施しない場合は減点対象**（つなぎの成立自体は妨げない）。
- 構成例：`ロンダート → 後方宙返り一回半ひねり → ロンダート → ダイビング前宙`
- アプリの判定：`宙返り → A難度つなぎ技 → 宙返り` のパターンを検出（手具操作の有無は問わない）。手具操作がない場合は別途減点対象として表示。
  - ※判定の起点（A難度始まりを有効とするか）は要確認事項として保留中。

### 技一覧（難度・系統）

| 難度 | 技名 | 系統 |
|------|------|------|
| A | 側転 | 側方系 |
| A | ロンダート | 側方系 |
| A | バク転 | 後方系 |
| A | ハンドスプリング | 前方系 |
| A | とび前転 | 前方系 |
| B | 側宙 | 側方系 |
| B | 後方宙返り | 後方系 |
| B | 後方屈伸宙返り | 後方系 |
| B | 後方伸身宙返り | 後方系 |
| B | 後方宙返り半ひねり | 後方系 |
| B | 後方伸身宙返り半ひねり | 後方系 |
| B | テンポ宙返り | 後方系 |
| B | ダイビング前宙 | 後方系 |
| B | 前宙 | 前方系 |
| B | 前宙半ひねり | 前方系 |
| B | きりもみ | 前方系 |
| C | 前方宙返り一回ひねり | 前方系 |
| C | きりもみ転回 | 前方系 |
| C | 後方1回半ひねり | 後方系 |
| C | 後方宙返り一回ひねり | 後方系 |
| C | 後方屈伸宙返り一回ひねり | 後方系 |
| C | 後方伸身宙返り一回ひねり | 後方系 |
| C | テンポひねり | 後方系 |
| D | 伸身前宙一回ひねり | 前方系 |
| D | 後方宙返り二回ひねり | 後方系 |
| D | 後方伸身宙返り二回半ひねり | 後方系 |
| D | 後方二回宙返り | 後方系 |
| E | 後方宙返り三回ひねり | 後方系 |
| E | 後方伸身宙返り3回ひねり | 後方系 |
| E | 後方伸身宙返り3回半ひねり | 後方系 |
| E | 後方伸身二回宙返り | 後方系 |
| E | 後方2回宙返り1回ひねり（ムーンサルト） | 後方系 |
| E | 伸身前宙二回ひねり | 前方系 |
| E | ダイビングダブル | 後方系 |
| E | ルドルフ | 後方系 |
| E | スワンダブル | 後方系 |

> A難度はすべてつなぎ技に使用可能（`isConnectA`）。A難度以外はすべて宙返り系（`isSalto`）扱い。

---

## 4. 加点

演技中の加点要素を列挙して合計する。必須要素と加点要素は**同時に実施**されうるため、実施要素のリストから両方を自動算出する。

| キー | 加点要素 | 点数 | 状態 |
|------|----------|------|------|
| `apparatusBonus` | 種具操作による加点 | 未設定 | 件数カウントのみ実装 |
| `throwBonus` | 投げによる加点 | 未設定 | 件数カウントのみ実装 |

> 点数・条件は未確定。確定次第ここに記載し、アプリの `BONUS_RULES` に反映する。

---

## 5. 連続技の難度判定

連続技の中で、起点技に対し後続技の難度に応じて格上げ（ランクアップ）する。

### 難度の数値対応
A=1, B=2, C=3, D=4, E=5

### 格上げルール
- **A難度**：起点にも後続にもならず**完全に無視**（つなぎ含む）。
- **起点**：A難度を除いた最初の技が起点（その技の難度値）。
- **後続**：起点以降の技について **(その技の難度値 − 1)** を累積加算。
  - B(2)→+1、C(3)→+2、D(4)→+3、E(5)→+4。
- **投げタン**：連続技内に投げを含む場合、**最終難度を +1**（連続技単位で適用）。
- **上限**：**E（5）で頭打ち**。

### 投げタンについて
- 「投げ」をA難度技としてプルダウンに用意（種具を投げる動作）。
- パターン：①投げ→技実施→キャッチ　②技実施中に空中で投げ→キャッチ。
- アプリ上の投げ認識：「投げ」技の選択（`isThrowSkill`） または 各技の「投げ」チェック（`isThrow`）のいずれか。
- 格上げは連続技の最終難度に対して +1。

### 計算式
```
基本値 = 起点(A除外後の最初の技の難度値) + Σ(後続技の難度値 - 1)
最終難度 = 基本値 + (投げを含むなら +1)
ただし上限 5（E）
```

### 検算例
| 構成 | 計算 | 結果 |
|------|------|------|
| 前宙(B) + 前宙(B) | 2 +1 | C(3) |
| 後方宙返り半ひねり(B) + 前宙(B) + 側宙(B) | 2 +1 +1 | D(4) |
| 後方宙返り一回半ひねり(C) + 前宙(B) | 3 +1 | D(4) |
| 後方1回半ひねり(C) + 前方1回ひねり(C) | 3 +2 | E(5) |
| 前宙(B) + ロンダート(A) + 後方伸身宙返り(B) + 前宙(B) | 2 +0 +1 +1（Aは無視） | D(4) |
| ロンダート(A) + 二回半ひねり(D) + ロンダート(A) + 一回半ひねり(C) | 起点D(4) +2（Aは無視） | E(5) |

- アプリ側は `SKILL_LIST` の `difficulty` フィールド（"A"/"B"/"C"…）を参照して算出する。

---

## 6. 団体（5人）モードのシリーズ難度

団体は「5人 × スロット」のグリッドで構成し、横方向に連続する非空セルを1つの**塊**として扱う（個人のタンブリング1本に相当。ただし投げ加点 +1 は無い）。各シリーズの難度は次で決まる。

```
シリーズ難度 = max( 三人以上が到達した難度, 各交差グループの合計難度, 各組運動グループの難度 )
団体D = Σ各シリーズ難度点 + 加点4カテゴリ（§6.4）
```

アプリ定義：`src/scoring/team.ts`（純粋関数）、UI は `src/components/TeamScorer.tsx`。

### 6.1 三人以上が到達した難度

- 各シリーズの難度は、**三人以上が実施した難度**を採用する。
- 実装：各レーン（選手）の最高塊難度を求め、5人分を**降順**に並べた**3番目**の値を採用難度とする。
  - 例：5人の難度が E, D, C, B, A → C以上を3人(E,D,C)が満たすので **C**。
- ただし後述の交差合計難度が3人難度を上回った場合はそちらを採用する。
  - 例：3人がA、残り2人が交差でDを獲得 → そのシリーズは **D**。
- 同時実施シリーズ（全員同一実施）は全員が同じ難度なので、その塊難度がそのまま採用難度。

### 6.2 5人同時に同じ技 → 難度を一つ上げる

- 5人が**同時に同じ技**を実施した場合、その技の難度を**一つ上げる**（上限E）。
  - 実装：同一スロットで全レーンが同じ `skillId` のとき成立。同時実施シリーズは全技が対象。
- ただし**連続した転回の途中**で同時に同じ転回を実施した場合は、
  **「塊全体の連続難度」** と **「その技を格上げした難度（技+1）」** を比較し、**高い方**を採用する。
  - 実装：塊の `adjValue = max(連続加算値, その技の難度値 + 1)`。

### 6.3 交差・組運動

#### 交差（二段・多段）
- **順序は無関係**。何の難度を使って飛んだか＝**選択した各段の難度値の連続加算**で決まる。
- 計算式（個人のタンブリング連続と同じ足し算）：

```
交差難度値 = Σ(各段の難度値) − (段数 − 1)        上限 5（E）
```

- 段の難度値：技はその難度値（A=1〜E=5）、**徒手系は値1**（＝+0、飛んだ転回の難度を採用）。
- 成立要件：**転回（技）を含む2段以上**。徒手のみ・1段では難度なし。
- 検算：

  | 構成 | 計算 | 結果 |
  |------|------|------|
  | A段の上を B で飛ぶ | 1+2 −1 | **B**（飛んだ難度を採用）|
  | B段の上を B で飛ぶ | 2+2 −1 | **C**（二段目の一つ上）|
  | C段の上を B で飛ぶ | 3+2 −1 | **D** |
  | C段の上を C で飛ぶ | 3+3 −1 | **E**（二段目の二つ上）|
  | 徒手の上を C で飛ぶ | 1+3 −1 | **C**（転回の難度を採用）|
  | 三段（B+B+B） | 2+2+2 −2 | **D**（一・二段目合計＋三段目を加算）|

- 三段以上：上式の Σ がそのまま全段の合計加算になるため、四段目以降も同様に処理される。
- UI：グリッドのセルを選択して交差グループを作る。1シリーズに**独立した交差を複数**作成可（交差1・交差2…）。

#### 組運動
- **転回を伴わない**場合は難度なし。
- 飛ばして**空中で転回（宙返り）**を行う場合、**最大C難度**まで採用（`min(宙返りの難度, C)`）。
- UI：交差と同様にグリッドのセルを選択して組運動グループを作る。選択した**宙返り技**（`isSalto`）の難度の最大値（上限C）が組運動難度。1シリーズに複数作成可。

### 6.4 団体の加点（D加点）

各カテゴリは**1演技で最高ティアのみ**採用し、4カテゴリを合算して D に加える。

#### 同じ転回技に関わる加点（最大0.3）
- ① 5名全員が**同じ技構成**で**4つ以上連続する転回**を実施（タイミングは不問）… **0.1**
- ② ①を**同時**に実施（同時実施シリーズ、または同じ技構成が**同一スロット起点**で全員揃う）… **0.2**
- ③ ②を**D難度以上**で実施（その同時塊の難度がD以上）… **0.3**
- 「連続する転回」は塊内の技セル数で数える（A難度のバク転等も1つと数える）。技構成は技の並び（`skillIds`）が一致すること。
  - 例：全員が `バク転→バク転→バク転→後方伸身宙返り` を同時実施 → 同一構成・技4つ・同時・塊難度はB（A無視）→ **0.2**。

#### 着地に関する加点（最大0.2）
- ① 5名全員が一つのシリーズの**終末**で、宙返りからの足裏着地で静止（**着ピタ**）… **0.1**
- ② ①の着地を**同時**に行う（全員の着ピタ終末技が**同一スロットに縦並び**、または同時実施シリーズ）… **0.2**
- UI：各塊の**最後の技**に「着ピタ」チェックボックス。全レーンの終末技にチェックが入ると成立。

#### 交差に関する加点（最大0.3）
- ① **すべてC難度以上**の転回で**3段以上**の交差 … **0.1**
- ② ①にD難度以上の転回が**1つ**含まれる … **0.2**
- ③ ①にD難度以上の転回が**2つ**含まれる … **0.3**

#### 同一難度に関する加点（最大0.2）
- ① 全員が一つのシリーズで**D以上**の転回を実施 … **0.1**
- ② ①を**E**で実施（全員がE以上）… **0.2**

> 加点定数は `constants.ts` の `TEAM_ROTATION_BONUS` / `TEAM_LANDING_BONUS` / `TEAM_CROSS_BONUS` / `TEAM_SAMEDIFF_BONUS`。

---

## 7. 未確定・要確認事項

- [x] 各技の正式な難度値（全25技を `SKILL_LIST` に設定済み）
- [ ] 加点の具体的な条件と点数
- [ ] 連続技の難度算出ロジック
- [ ] 不足要素に対する減点の扱い
- [x] 団体モードのシリーズ難度（3人到達・5人同時・交差・組運動／§6）と団体加点4カテゴリ（§6.4）— A/E実施減点は今後
- [ ] 正式な技名一覧（現在の `SKILL_LIST` は仮）

---

## 8. アプリ定義テーブルとの対応

| ドキュメント項目 | アプリ内の定義 |
|------------------|----------------|
| 技リスト・系統・難度 | `SKILL_LIST` |
| 必須要素チェック | `REQUIRED_CHECKS` |
| 加点ルール | `BONUS_RULES` |
| 系統タグ | `CATEGORY` |
