# 採点計算アプリ 実装仕様書

`mens-rg-rules.md`（公式規則）とアプリ内の実装（`src/scoring/`）を対応付けるドキュメント。
規則の変更は `mens-rg-rules.md` に、アプリ固有の仕様は本書に記載する。

---

## 1. 実装状況

| モード | 規則セクション | 状態 | 備考 |
|--------|---------------|------|------|
| 個人（個人競技） | §3.5 | 実装済み | D/A/E すべて |
| 団体（自由演技） | §3.4 | D・A・E実装済み | E実施減点は各シリーズ／演技全体で手入力。A減点は暫定精度 |
| 個人選手 | §3.8 | 未実装 | 団体内個人の別採点方式（A=3.00満点） |

---

## 2. 採点カテゴリとアプリの計算

**合計 = D + A残点 + E残点**（A・Eの残点は0で止まる）

- **D**：タンブリング難度点、徒手難度点、連続投げ加点、技術加点、手具操作加点、二つ投げ4動作加点、様々な跳び加点。
- **A**：方向系不足減点、投げ回数不足減点、連続宙返り減点、つなぎ技手具操作なし減点、無手具操作減点、投げ/受け種類不足減点。
- **E**：各シリーズに自由入力する実施減点 + 演技全体の実施減点の合計（出来栄え判定はユーザー入力）。

アプリの single source of truth：`computeScore()` in `score.ts`

---

## 3. 定義テーブル対応表

| 規則の項目 | 定義名 | ファイル |
|-----------|--------|---------|
| 技リスト・系統・難度 | `SKILL_LIST` | `constants.ts` |
| 難度点（A=0.1〜E=0.7、十年後モードのF=0.9・G=1.1） | `DIFF_SCORE` | `constants.ts` |
| 難度の数値対応（A=1〜E=5、F=6・G=7） | `DIFF_VALUE` / `VALUE_DIFF` | `constants.ts` |
| 難度上限（現行規則） | `MAX_DIFF = 5` | `constants.ts` |
| 適用中の難度上限（十年後モード） | `maxDiff(future)` / `clampDifficulty()` | `constants.ts` |
| 位置で変わる難度（ダイビングの格上げ） | `skillDifficultyAt()` | `constants.ts` |
| 系統タグ（前方/側方/後方/その他） | `CATEGORY` | `constants.ts` |
| 手具定義 | `APPARATUS` | `constants.ts` |
| 手具ごとの必須投げ | `REQUIRED_THROW_OPTIONS` | `constants.ts` |
| 投げオプション | `THROW_OPTIONS_COMMON` / `THROW_OPTIONS_APPARATUS` | `constants.ts` |
| 受けオプション | `CATCH_OPTIONS_COMMON` / `CATCH_OPTIONS_APPARATUS` | `constants.ts` |
| 手具が二つあるか | `APPARATUS_USE` / `APPARATUS_COUNT` | `constants.ts` |
| 徒手動作（1〜4動作 + 縦3動作） | `HAND_MOTIONS` | `constants.ts` |
| 徒手動作の選択肢（動作数 + 徒手扱いの転回技） | `MOTION_OPTIONS` / `MOTION_SKILLS` | `constants.ts` |
| ロープ跳びの難度表 | `ROPE_JUMPS` | `constants.ts` |
| 徒手系難度表（§3.6.1 跳躍・バランス・倒立・柔軟） | `HAND_ELEMENTS` / `HAND_ELEMENT_GROUPS` | `constants.ts` |

### タンブリング技の入力パターン

技は**一覧から選ぶ**ほかに、**ひねり回数と姿勢で組み立てる**こともできる（シリーズカードの技ブロックの
「手動入力に切り替え／一覧入力に切り替え」ボタンで切り替え）。

- 選択肢：`TWIST_BASES`（後方宙返り／前宙）× `POSTURE_OPTIONS`（抱え込み・屈伸・伸身）×
  `TWIST_OPTIONS`（なし〜3回半ひねり、0.5刻み。十年後モードでは `twistOptions(future, …)` が
  `FUTURE_TWIST_OPTIONS` ＝4回〜5回ひねりまで伸ばす）
- 難度は §3.6.2 の表どおり `twistDifficulty()` で決める
  - **後方系は姿勢によらずひねり回数だけ**：0・半＝B／1回・1回半＝C／2回・2回半＝D／3回以上＝E（#7・#12〜#17）
  - **前方系は伸身が1段階上**：かかえ込み・屈身は後方系と同じ表、伸身は 0＝C／1回・1回半＝D／2回以上＝E（#8・#11〜#14）
- idは `buildTwistSkillId()` が組み立てる。**`SKILL_LIST` に同じ内容の技があればそのidを返す**ので、
  一覧から選んでもひねり指定で選んでも同じidになり、重複判定（§3.4.4）が食い違わない。
  一覧に無い組み合わせ（前方伸身宙返り、後方屈伸宙返り半ひねりなど）だけ `tw:<base>:<twist>:<posture>` の
  合成idになり、`skillDef()` が `twistName()` / `twistDifficulty()` から技として解決する
- 既存idからは `parseTwistSkillId()` でひねり・姿勢に戻せる（`Skill.twist`）。組み立てで表せない技
  （側宙・テンポ・きりもみ・2回宙返り系など）は `null`
- ジュニアの難度認定は合成idにも効く（後方宙返り半ひねりは姿勢を問わずC）

入力パターンは**技ブロックごと**に持ち、**既定は一覧**（1つを切り替えても他の技は一覧のまま。
ブロックをまたいでも再読み込み後も覚えない）。一覧から編集できない合成id（`tw:`）だけ手動入力で開く。

### 実施できる向き（選択肢の絞り込みとロンダートの自動挿入）

立った状態からいきなり実施できる宙返りは前方系と側宙だけで、後方系はロンダート・バク転から入る。
入力を楽にするため、**選択肢は隠さずロンダートを自動で挟む**方向で実装している（採点には影響しない、入力補助）。

- `leadsBackward(prevSkillId)` … その技が**進行方向に対して後ろ向きで終わる**か。
  - ロンダート・バク転（`BACKWARD_ENTRY_SKILLS`）→ true
  - 後方の宙返り → ひねりなし／整数ひねりは true。n回半ひねり（`twist` の小数部が0.5）と
    ダイビング前宙（`FORWARD_LANDING_BACK_SALTOS`）は前向きで終わるので false
  - **前方の宙返り → n回半ひねりだけ true**（前方で半ひねりすると後ろ向きで終わるので、
    ロンダートを挟まずそのまま後方系に入れる）
  - 側転・側宙・ハンドスプリングなど → false
- `skillFlowAfter(prevSkillId)` → `SkillFlow`（`backward` / `forward` / `side`） …
  **ロンダート・バク転からは後方系しか選べない**。それ以外の位置は全部選べる
  （宙返りの後に前方系へ戻るのは実用的ではないが可能）。
  `skillOptions(junior, flow)` / `skillOptionGroups(junior, flow)` が絞り込みを行い、
  既に入っている技が選択肢から外れるときは選択値として残す（ジュニア禁止の技と同じ扱い）

  | 直前の技 | 後方系 | 前方系 |
  | --- | --- | --- |
  | ロンダート・バク転 | 可 | 不可（非表示） |
  | 前方の半ひねり系 | 可 | 可（実用的ではない） |
  | 前方の0〜整数ひねり | ロンダートを挟む | 可 |
  | 後方の半ひねり系・ダイビング前宙 | ロンダートを挟む | 可 |
  | 後方の0〜整数ひねり | 可 | 可 |

- `needsRoundoffBefore(items, iIdx)`（`analysis.ts`） … 後方系を選んだ位置が `leadsBackward` を
  満たさないとき true。`SeriesListEditor` の `updateItem` がこれを見て手前に `roundoffItem()` を挿し込む。
  何も無いところでの**立ちバク転はそのまま**（宙返りのときだけ補う）
- ひねり指定のベース（後方宙返り／前宙）も同じ `flow` で絞る
- 団体モードは同じ `skillFlowAfter` で絞るだけ（グリッドなのでロンダートの自動挿入はしない）

UIのプルダウンは見出し（`optgroup`）で分類する。タンブリング技は**前方系・側方系・後方系**
（`skillOptionGroups(junior, flow)`、系統の順は `SKILL_CATEGORY_ORDER`）、徒手動作は
**縦回転・横回転**（`motionOptionGroupsFor(prevMotionId)`、`MOTION_AXIS_GROUPS`）。徒手扱いの
転回技はすべて縦の一回転なので縦回転側に入る（`motionDef()` の扱いと同じ）。群の中の並び順は
それぞれ `SKILL_LIST` / `motionOptionsFor()` の優先順を保つ。

タッチダウンライズは**縦回転の徒手としてのみ**判定するため、`SKILL_LIST`（タンブリング技）には入れず
`HAND_MOTIONS` に1動作の項目（`td_rise`）として持つ。方向系の網羅判定にも影響しない。

`ROPE_JUMPS` は §3.5.5.3 の跳び難度表と1対1。1重跳びは全パターンA固定のためクロスの区別を持たない。
3重跳び連続3回以上（D）・4重跳び（D）・4重跳び連続2回以上（E）は**難度判定で前後を区別しない**ため、
前後で同じ難度のエントリを両方持つ（`3x3f`/`3x3b`、`4f`/`4b`、`4x2f`/`4x2b`）。
`direction` は §3.2(3) の前回し／後ろ回し跳び2回以上連続の要求要素判定にのみ使う。

---

### 位置で難度が変わる特例（ダイビング）

**ダイビング**（`DIVING_SKILL_ID` = `a_diving`、後方系）は頭から着地する技で、
**この技の後に技は続かない**。単発では **A難度**だが、**宙返りの直後に連続で実施すると
1段格上げして B難度**として認定する（`DIVING_UPGRADED_DIFFICULTY`）。
間にA難度技（ロンダート・バク転）が入ったら「連続」ではないので格上げしない。

難度が**位置で変わる**のはこの技だけなので、並びから引く関数を分けてある：

| 用途 | 関数 |
| --- | --- |
| 位置に依存しない難度（表の値。ジュニア・十年後モードの読み替えを含む） | `skillDifficulty(id, junior, future)` |
| 並びのなかでのその位置の難度（ダイビングの格上げを反映） | `skillDifficultyAt(skillIds, i, junior, future)` |

連続の難度を出す `calcTumblingDifficulty`（個人）と `calcChunkDifficulty`（団体）は
後者を位置ごとに引く。宙返りとしては**常に数える**（`isSalto: true`）ので、
宙返り→宙返り→ダイビング は三宙になる。
自動生成では組み立てない（`Skill.noAuto`。入力画面の選択肢には出す）。

### 技の最中の投げ（投げタン）の必須投げ

`SkillItem` は投げアイテムと同じく **`reqTypes`** を持てる（`isThrow` のときだけ有効）。
入力画面では「この技の最中に投げ」を選ぶと**二つ投げ**のチェックが出る（クラブ・リングのみ。
手具を使った投げとは排他）。投げアイテムの `reqTypes` を見ているところはすべて
技の最中の投げも見る：

| 見ているもの | 効き方 |
| --- | --- |
| `thrownCount(item)` | 二つ投げなら手元から2つ離れる。`handsEmptyFlags` / `catchTwoFlags` / `checkApparatusFlow` が共有する |
| `performedThrowTypes`（`score.ts`） | 手具別必須要素の **2つ同時投げ（自動判定）** を満たす |
| 二つ投げの徒手動作加点 | 技の最中の二つ投げから受けるまでの徒手動作も数える |
| `seriesSignature` | 二つ投げの有無で別のシリーズになる |
| `apparatusBlockers` / `stripForApparatus` / `commonBlockers` | その手具で入力できない必須投げは落とす |

自動生成は**この形を作らない**（`canTwoThrowTumbling` は投げてから跳ぶ形だけを許す）。
入力はできるが、候補としては提案しないという住み分け。

## 4. 個人モードの加点定数

| 規則の加点項目 | 定数名 | 値 | 条件 |
|--------------|--------|-----|------|
| シリーズ加点 | `SERIES_BONUS` | 0.1 | 投げ2回以上 & D以上のユニットあり |
| 技術加点（投げ/受け1つにつき） | `TECHNIQUE_BONUS` | 0.1 | 視野外/手以外/手具使用の投げ・受け |
| 手具操作加点 | `APPARATUS_OP_BONUS` | 0.1（演技全体で最大0.1） | 最高難度E & （手具操作2回以上 **または** 手具を保持した技の最中に投げ）。2つは同じ加点なので重複しない |
| 二つ投げ4動作加点 | `TWOTHROW_MOTION_BONUS` | 0.1 | 二つ投げ中に徒手4動作以上 |
| 様々な跳び加点 | `JUMP_VARIETY_BONUS` | 0.1 | ロープ：6m移動連続跳びに2重跳び3回以上（§3.5.5.5(4)①）。②③は入力未対応 |

---

## 5. 個人モードのA減点定数

| 規則の減点項目 | 定数名 | 値 | 条件 |
|--------------|--------|-----|------|
| 方向系不足 | `DIRECTION_DEDUCTION` | 0.3 | 前方/側方/後方の不足1方向につき（徒手扱いの技＝側転は数えない） |
| 投げ回数不足 | `THROW_COUNT_DEDUCTION` | 0.3 | 投げが `THROW_COUNT_REQUIRED`（一般3／ジュニア2）未満 |
| 投げ回数超過 | `THROW_COUNT_OVER_DEDUCTION` | 0.3 | ジュニアのみ。`JUNIOR_THROW_COUNT_MAX`（5回）を超えた1回につき |
| つなぎ技手具操作なし | `CONNECT_NO_APP_DEDUCTION` | 0.2 | つなぎ技のA難度で手具操作なし（投げなしタンブリング塊のみ・Q&A Q10） |
| 宙返り2連続止まり | `SALTO_CHAIN_2_DEDUCTION` | 0.1 | 最大連続宙返りが2 |
| 宙返り連続なし | `SALTO_CHAIN_LOW_DEDUCTION` | 0.2 | 連続宙返りなし |

**きりもみ系の宙返り判定（Q&A Q7 / 規則集 P51 3.6.2.4 注釈）**：きりもみ・きりもみ転回
（`Skill.saltoOnlyInChain`）は**宙返りの連続に含まれる場合のみ**宙返りとして数える。
判定は `saltoFlags(skillIds)` に集約し、`maxSaltoChain` / `hasConnect` /
`hasConnectWithoutApparatus` / 無手具操作減点の宙返り判定がこれを使う。
- 隣（つなぎ技のA難度技は読み飛ばす）に本物の宙返りがあれば宙返り扱い
- きりもみ同士が並んだだけでは連続とみなさない（本物の宙返りが必要）
- 難度そのもの（きりもみB／きりもみ転回C）は変わらない

**宙返りに数えない技は徒手系の動作として数える（Q&A Q7 の回答）**
- `tumblingFlags(skillIds)` が転回系として扱う技（宙返り＋宙返り間に挟んだつなぎ技のA難度技）を返し、
  それ以外の技は `handMotionsOfSkill()` の動作数として徒手系難度に合算する
  - A難度技（側転・ロンダート・バク転・ハンドスプリング・とび前転）＝縦の一回転の徒手で **1動作**
  - **側転は徒手扱い**（`Skill.isHandElement`）：つなぎ技にならず（`isConnectA` なし）、
    方向系の判定にも数えない。徒手動作としては従来どおり選べる
  - きりもみ（B）＝1動作、きりもみ転回（C）＝2動作（難度をそのまま徒手系難度に読み替え：`max(1, 難度値 − 1)`）
- 徒手動作（`motion` アイテム）と技由来の動作数は**合算**する。
  例：投げ→2動作→バク転→キャッチ ＝ 3動作 → D難度
- 転回系の技が1つも無い塊は `isThrowTumbling = false` の徒手系ユニットになる。
  投げを含まない場合も徒手系ユニットとして難度を数える（`Unit.isThrow = false`／型は `"throw"`）
- 例：投げ→バク転→キャッチ ＝ 徒手系B、投げ→きりもみ転回→キャッチ ＝ 徒手系C、側転単体 ＝ 徒手系B
- これらの技は**徒手動作アイテムの選択肢にも出す**（`MOTION_OPTIONS`＝`HAND_MOTIONS` + `MOTION_SKILLS`）。
  タンブリング技として入れても徒手動作として入れても動作数は同じ（`motionDef()` が両方を解決する）
- 徒手動作の選択肢は**回転系の徒手のみ**（`MOTION_OPTIONS`）。投げ受けの間に数えるのは
  §3.5.5.3 の注釈どおり縦軸・横軸の360度回転だけなので、汎用の「n動作」（`m1`〜`m4`・`mv3`）は
  `legacy: true` にして選択肢から外した。保存済みデータは `legacyMotionDef()` で解決し、
  読み込んだ構成では「3動作（旧）」のように選択値として表示・計算する
- 選択肢の並びは `motionOptionsFor(prevMotionId)`：既定は `MOTION_PRIORITY_DEFAULT`
  （シェネ→前転→側転→とび前転→ハンドスプリング）、直前に選んだ動作があれば
  `MOTION_PRIORITY_AFTER`（シェネの次＝前転・転がり・側転／前転の次＝転がり）を先頭に足す。
  並べ替えるだけで選択肢の顔ぶれは変わらない
- 徒手動作アイテムは**連続回数**（`MotionItem.count`、既定1）を持ち、動作数は回数分だけ加算される。
  「1動作×2回」と「2動作」は内容キーまで含めて同じ扱い。
  回数は**0まで下げられる**（入力欄を消した状態＝0）。`motionTimes()` は未指定を1回、0以下を0回とし、
  **0回の動作は難度にも技の構成（内容キー）にも数えない**。UIでは0を空欄で表示するので、
  消してそのまま入力し直せる
- 縦の一回転の徒手：タッチダウンライズ・前転・後転・ギャンビ（各1動作、`vertical`）。
  これらと徒手扱いの転回技（側転・バク転・きりもみ系など）が3動作分そろうと縦3動作＝E
- シェネは**横の一回転**（1動作・縦回転には数えない）。転がりも同じ。
  シェネは `hasHandsOption` を持ち、`MotionItem.hands` で手ありを区別する。
  手ありのときは種類（`MotionItem.handsType` ＝ `HANDS_TYPES`：片手上げ／両手上げ／回旋／その他、
  既定は片手上げ）も選ぶ
  - 手あり／手なしで**別の技**として扱う（Q&A Q28）。手ありは種類ごとに別の技
  - 種類「その他」を含むユニットは `Unit.neverDuplicate` が立ち、いくつあっても重複と見なさない
    （「その他の投げ／受け」と同じ扱い）
  - 手ありと手なしが混在した塊は、手なし・手ありのどちらの内容とも同じ技として扱うため
    内容キーを複数持つ（`Unit.signatures`、Q&A Q21）。手ありのキーは `:h:<種類>` を付ける。
    採用は難度点の高い順に走査し、どちらかのキーが埋まっていれば不採用
- **縦回転の徒手が3動作分そろうと縦3動作＝E難度**として扱う（`HandMotion.vertical` /
  徒手扱いの転回技はすべて縦回転）。例：投げ→側転→バク転→タッチダウンライズ→キャッチ ＝ E。
  一般の動作数（1〜4動作）は縦回転として数えないので、2動作＋バク転は3動作のD止まり
| 投げなしタンブリング（宙返り系のみ） | `NO_APP_SALTO_DEDUCTION` | 0.1 | 宙返り系すべてに手具操作なし |
| 投げなしタンブリング（全体） | `NO_APP_ALL_DEDUCTION` | 0.2 | シリーズ全体に手具操作なし |
| 無手具操作の上限 | `NO_APP_CAP` | 0.4 | 演技全体での合算上限 |
| 投げ方/受け方の多様性不足 | `VARIETY_DEDUCTION_PER` | 0.1 | 1種類不足につき |
| 多様性不足の上限 | `VARIETY_CAP` | 0.5 | 投げ方+受け方の合算上限 |
| 必要種類数 | `VARIETY_REQUIRED` | 3 | 投げ方・受け方それぞれ |
| 手具別必須要素の欠如 | `REQUIRED_ELEMENT_DEDUCTION` | 0.3 | §3.2 手具操作要求要素の未実施1つにつき（§3.5.6.3） |
| 必須要素の欠如 | `MISSING_ELEMENT_DEDUCTION` | 0.3 | 投げタン・つなぎ技・タンブリング本数の不足（各） |
| 違反・欠如 | `VIOLATION_DEDUCTION` | 0.3 | 開始/終了/音楽違反・徒手系基礎要素群欠如の各該当（§3.5.6.3） |
| 芸術と多様性の欠点 | `ART_DEDUCTION_ITEMS` | 0〜各項目の上限 | §3.5.6.4 の欠点テーブル（審判の主観評価・手入力） |

`ART_DEDUCTION_ITEMS` は §3.5.6.4 の欠点テーブルと1対1（`id` / `group` / `name` / `max` / `note`）。
値は `clampArtDeduction(id, v)` で 0〜`max`（0.1刻み）に丸め、合計を `ScoreResult.artDeduction`、
全項目の内訳を `artRows` として返す。UIは「芸術と多様性の欠点（§3.5.6.4）」カードで項目ごとに
プルダウン選択し、`SaveData.artDeductions`（項目id → 減点）としてファイル/テキスト/共有URLに往復する。
表のうち**「投げ受けの操作」（上限0.50）だけはシリーズ入力から自動判定**する（投げ方・受け方の種類不足）ため
手入力の対象から外している。

**「転回系の種類・組み合わせの多様性」（`tumVariety`、上限0.50）は裏で自動計算する**（`tumblingVariety`）。
上級者が必須要素をすべて満たす構成では、ロンダート等のA難度を含めて9〜11個の転回が入り、A難度を除くと
**6〜8個の宙返り**になる。それが全部違う技なら減点なしで、**同じ技を繰り返したぶん**
（`repeats`＝実施数−種類数）1つにつき `TUM_VARIETY_DEDUCTION_STEP`（0.1）を引く（上限0.50）。
A難度の転回技（ロンダート・バク転・側転）は数えず、徒手動作として入れた転回技も数えない。
ひねり・姿勢が違えば別の技（`skillId` が違う）として数える。
自動計算した値は `ScoreResult.tumVariety`（`total` / `distinct` / `repeats` / `deduction`）として返すだけで、
**A減点には自動では入れない**：UIは項目の横に「自動計算」ボタンを出し、押すと手入力欄にその値が入る
（宙返りの個数と種類数も項目の注記に出す）。
ランダム生成では `generate.ts` の評価がこの自動計算値を**そのまま減点として負う**ので、
**上級者（難度の高い構成）になるほど宙返りの種類が増えて減点が小さくなる**。
実測（20構成ずつ・stick）：宙返りの数／種類 上限1.5点 3.6個・3.3種類／2.5点 6.7個・6.1種類／
3.5点 7.6個・6.8種類／4.5点 7.9個・7.4種類／上限なし 8.1個・7.7種類。
減点の平均は評価に入れる前 0.09／0.12／0.11／0.10／0.06 → 入れた後 0.03／0.06／0.07／0.05／0.04
（Dスコアの平均は変わらない）。

`REQUIRED_ELEMENT_DEDUCTION` の対象要素は `APPARATUS_REQUIRED_ELEMENTS`（手具別）。
左手投げ/二つ投げ・3回以上の投げ上げは既存判定（必須投げ・投げ回数）と重複するため対象外。
要素に `auto` が付いているものはシリーズ入力から自動判定し、手動チェック欄には出さない（手動チェックでは上書きできない）。

| 要素 | `auto` | 判定 |
|------|--------|------|
| スティック `stick_right`（右投げ右受け1回以上） | `rightThrow` | 左手投げ（`reqTypes: lefthand`）でも手以外の投げ（`throwTypes: nonhand`）でもない投げが1回以上あるか。技の最中の投げ（投げタン）も対象 |
| 各手具 `*_rotthrow`（転回系の投げ受け） | `throwTumbling` | 投げタン（`Unit.isThrowTumbling`）が1本以上あるか。必須要素チェックの `throwTum` と同じ判定 |
| スティック `stick_left` / リング・クラブ `*_twothrow` | `leftThrow` / `twoThrow` | 投げアイテムの必須投げ（`reqTypes`）に左手投げ／二つ投げがあるか |
| ロープ `rope_triple` / `rope_moving` / `rope_front` / `rope_back` | 同名の auto | ロープ跳びの入力から判定（3重跳び／6m以上移動の3回以上連続跳び／その場前回し・後ろ回し2回以上） |

`VIOLATION_DEDUCTION` の対象は `VIOLATION_OPTIONS`（審判判断による手動チェック）。

つなぎ技の手具操作なし減点（Q&A Q10）：後方一回半ひねり(操作なし)〜ロンダート(操作なし)〜ダイビング前宙(操作あり)
＝ A審判 −0.20（E審判の −0.10 は実施減点なのでユーザー入力）。操作は回しに限らず持ち替え・足やわきに挟むなども
含み、1つでもあれば減点しない（`hasApparatus` のチェック1つで表現）。対象は投げを含まないタンブリング塊のみ。
どちらも個人モードの routine レベル state（`apparatusElements` / `violations`）で保持し、`SaveData`・共有URLに含める。

---

## 6. 個人モードの採点パイプライン

```
series[] → analyzeSeries() → analysis[] → computeScore() → ScoreResult
```

### 6.1 `analyzeSeries(series)` in `analysis.ts`

items を左→右に走査し、`catch` が来たら buffer を flush して**ユニット**に変換する。
**タンブリングの合間の徒手（側転・徒手動作）でも区切る**（`unitSplitFlags`）：
前宙→前転→前宙 は「前宙（タンブリング）／前転（徒手）／前宙（タンブリング）」の3つとして
それぞれ評価する（連続宙返りもそこで切れる）。徒手の前後どちらかに転回技が無いとき
（着地の前転など）と、**投げ上げている間**（投げ〜キャッチの間で徒手とタンブリングが混ざった場合）は
区切らず、従来どおり1つの塊のまま評価する。つなぎ技（ロンダート・バク転・ハンドスプリング）は
転回技なので区切らない。

- 投げなし → `tumbling` ユニット（`calcTumblingDifficulty` で格上げ算出）
- 投げあり → `throw` ユニット（`max(handDiff, tumblingDiff)` を採用）
  - skill も含む場合は `isThrowTumbling`（投げタン）→ タンブリング枠に計上

### 6.2 `computeScore(series, apparatus)` in `score.ts`

1. 重複シリーズ判定（`seriesSignature` + `dupFlags`）
2. 各シリーズ内訳（`seriesBreakdowns`）を先に算出
3. グローバル値はシリーズ内訳を合算（二重実装防止）
4. 必須要素チェック → `required[]` / `missing[]`
5. `ScoreResult` を返す

### 6.3 重複シリーズの扱い

- `seriesSignature` でシリーズの構成を文字列化し、既出と一致するものを検出（`dupSignatureFlags`）
- `Series.notDuplicate` が true のシリーズは重複扱いを解除（`dupFlags = dupSignatureFlags && !notDuplicate`）
  - シェネの腕の使い方・動作の内訳（4シェネ／3シェネ＋前転 など）といった入力項目に現れない差異を
    ユーザーが宣言するためのフラグ。`SeriesCard` は `dupSignatureFlags` が立っている間だけ
    チェックボックスを出す（チェック後も消えないよう、表示条件は解除前の生の判定を使う）
- 重複シリーズ（`dupFlags`）はカウント・ボーナスに加えて **D にも一切算入しない**
  （難度点・連続投げ加点・技術加点・手具操作加点・二つ投げ4動作加点すべて0。
  §3.5.5「全く同じ技は難度として数えない」）。難度点の採用候補（top-3）からも除外する
- A側の判定（方向系・連続宙返り・つなぎ技・必須要素チェック）は重複シリーズのユニットも見る
- `throwOtherCount` / `catchOtherCount`（「その他」の投げ/受け）は重複でもカウント

### 6.3.1 シリーズ内訳の表示

- `SeriesBreakdown.tumRows` / `handRows` はユニット1つ分の行（`label` / `diff` / `score` / `adopted` / `inTop`）。
  タンブリング塊ごと・投げごとに1行出す。`adopted && inTop` の行だけが `tumDiff` / `handDiff` に合計される
- 採用されなかった行は `SeriesCard` が斜線＋「難度不採用」（同じ内容を既に採用）／
  「上位3つ外」（候補には入ったが上位3つに届かなかった）バッジで表示する
- `tumDiff` / `handDiff` は**上位3つに入ったユニットのみ**の合計なので、
  Σ各シリーズの `tumDiff` = `tumblingScore`、Σ`handDiff` = `handScore` になる（`dPart` も実際のDと一致）

### 6.4 採用数

- タンブリング難度：上位**3つ**を採用（`ADOPT_COUNT = 3`）
- 徒手難度：上位**3つ**を採用（採用候補は `adoptedHandUnits()` in `score.ts`）
  - 連続投げの2回目以降も難度点の候補に入る。難度が低ければ上位3つから漏れるだけで、
    採用対象から外すわけではない
- **同じ内容の難度は演技全体で1回しか数えない**（§3.4.4「全く同じ技は難度として数えない」）
  - 各ユニットは `Unit.signature` で内容を表す
    - 技を含むユニット（タンブリング塊・投げタン）＝転回系：**難度に効く非A難度技の並び**。
      手具を持っての前宙と投げての前宙は「同じ前宙」なので、投げの有無やA難度技は含めない（Q&A Q22）
    - 技を含まない投げ受け＝徒手系：投げとキャッチの間の**内訳**（動作の種類 → 実施回数）。
      順序は問わないが、内訳が違えば別の技（4シェネ と 3シェネ＋前転 は別）。
      徒手として数える技（側転・バク転など）はタンブリング技として入れても徒手動作として
      入れても同じキーになる。旧データの汎用動作（1〜4動作）は種類を区別できないため
      まとめて `m:<動作数>` として数える
    - ロープ跳び：跳びのid
    - 技術タグ（視野外・手以外・背面投げ等）はいずれも含めない
  - 同じ `signature` が複数あるときは**難度点の高いものだけを採用**する（同点なら先に実施した方）。
    例：前宙(B) → 投げ前宙(C) なら後者が採用（Q22）
    **不採用でも「実施しなかった」扱いにはしない**：技術加点・連続投げ加点・投げ回数・タンブリング本数・
    A側の判定（方向系・連続宙返り・つなぎ技・必須要素）には従来どおり算入する
  - `notDuplicate` を立てたシリーズのユニットは内容キーをシリーズ単位に閉じ、他シリーズと重複しない
  - 採用可否は `ScoreResult.unitAdopted[series][unit]` で返し、`SeriesCard` が「難度不採用」バッジを出す
  - 上位3つに入ったかは `ScoreResult.unitInTop[series][unit]`
  - 例（Q20）：3動作の投げ受け → 同じ3動作から背面投げで連続投げ
    ＝ 徒手難度点は D 1つ分（0.5）、背面投げの技術加点 0.1 と連続投げ加点 0.1 は付く
- A/E の満点：各**10点**（`AE_FULL = 10`）

---

## 7. 個人モードの必須要素チェック

`computeScore` が返す `required[]` の各項目：

| key | ラベル | 判定ロジック |
|-----|--------|-------------|
| `dir` | 前方系・側方系・後方系をすべて含む | タンブリングユニットの技の `category` 集合 |
| `throwTum` | 1本以上が投げタン | `isThrowTumbling` なユニットの存在 |
| `triple` | 1本以上が宙返り3回以上連続 | `maxSaltoChain >= 3`（`saltoFlags` 経由） |
| `connect` | 1本以上がつなぎ技 | `hasConnect()` で宙返り→A難度→宙返り パターン検出 |
| `count3` | 投げをN回以上実施 | `totalThrowCount >= requiredThrowCount`（一般3／ジュニア2。ラベルもNに追従） |
| （手具別必須投げは §3.2 手具別必須要素に統合） | — | `appThrow` は廃止し、`stick_left` / `*_twothrow` の自動判定で扱う |
| `countMax` | 投げはN回以内（ジュニアのみ） | `performedThrowCount <= maxThrowCount`（5） |
| `tumCount` | タンブリング3本以上 | `nonDupTumblingCount >= 3` |
| `appThrow` | 手具別必須投げ | `REQUIRED_THROW_OPTIONS` の全IDが実施済みか |

各行は `deduction`（不足時のA減点）を持ち、`SeriesCard`／`ScoreSummary` が金額を表示する。
うち **投げタン・つなぎ技・タンブリング本数**の不足は `missingElementDeduction`（各 −0.30）として
A減点に加算する。方向系・連続宙返り・投げ回数は従来どおり固有の減点項目で計上するので二重計上しない。

`required[]` とは別に、`computeScore` は §3.2/§3.5.6.3 用の表示リストも返す：
- `apparatusElementChecks[]`：`APPARATUS_REQUIRED_ELEMENTS[apparatus]` の手動チェック状況（未実施は `apparatusElementDeduction` に −0.3）。
- `violationChecks[]`：`VIOLATION_OPTIONS` の該当状況（passed=違反なし。該当は `violationDeduction` に −0.3）。

---

### 7.5 団体の徒手（個人とは別物）

団体には手具が無いので、個人の「投げ受けの間の動作数（1〜4動作）」は使わない。団体の徒手は
**§3.6.1 徒手系難度表**（跳躍12・バランス8・倒立9・柔軟7＝計36項目、`HAND_ELEMENTS`）から選び、
難度は表の**団体（5名実施）列**（`teamHandDifficulty()`）を採用する。

- 徒手セルは**連続しない**：前後のセルと塊を作らず単独の塊になる（技どうしも徒手を挟むと繋がらない）
- 徒手は**全員実施が前提**。シリーズは `TeamSeries.content`（`"skill"`＝転回 / `"motion"`＝徒手）で
  どちらの内容かを持ち、UIでは**同時実施シリーズの「転回／徒手」切替**で選ぶ。
  徒手シリーズは `mode: "allTogether"` 固定・1レーン・1スロットで、徒手要素を1つだけ選ぶ
  （`motionSeries()`）。グリッド・交差・組運動は表示しない
- 旧データ（`content` 無し）で徒手セルを含むシリーズは、`normalizeTeamState` が最初の徒手要素を拾って
  徒手シリーズに正規化する。採点ロジック側はセル位置を問わず計算できるままなので、
  途中スロットに徒手がある構成もそのまま採点できる
- 団体列がすでに5名実施の値なので、5人同時の**格上げ（+1）は行わない**
- 交差グループの段の値も表の難度値を使う（従来は一律1）

---

## 8. 団体モードの定数

| 規則の項目 | 定数名 | 値 |
|-----------|--------|-----|
| 組運動の最大難度 | `UNION_MAX_VALUE` | `DIFF_VALUE.C` (= 3) |
| 加点対象の連続転回数 | `ROT_CHAIN_REQUIRED` | 4 |
| 同じ転回技加点 | `TEAM_ROTATION_BONUS` | `{ all5: 0.1, sim: 0.2, simD: 0.3 }` |
| 着地加点 | `TEAM_LANDING_BONUS` | `{ all5: 0.1, sim: 0.2 }` |
| 交差加点 | `TEAM_CROSS_BONUS` | `{ base: 0.1, oneD: 0.2, twoD: 0.3 }` |
| 同一難度加点 | `TEAM_SAMEDIFF_BONUS` | `{ d: 0.1, e: 0.2 }` |

### 団体の採点ロジック

`computeTeamScore(team)` in `team.ts`

- グリッド構造：`Series[]` × player `lanes[]` × `slots[]` の `Cell`
- 横方向に連続する非空セルが1つの**塊**（個人のタンブリング1本に相当、ただし投げ加点+1は無い）
- A減点は `aDeductions` で算出（`missing.length * 0.3` は暫定）
  - **複雑な同調性タンブリング**は「同一スロットで**2〜4人**が同時に**転回系**を実施」を1回と数える
    （演技全体で 0回 −0.2 / 1回 −0.1 / 2回以上 0）。宙返りに限らず**A難度の転回**
    （ハンドスプリング・ロンダート等）も数える。5人そろったスロットは
    「全員同時のタンブリング」の対象なのでここでは数えない
- E実施減点は未実装

---

## 8.4 十年後モード（F・G難度／個人・団体）

**現行規則には無い仮想ルール**。十年後の男子新体操を想像するためのモードで、E難度の上に
F難度（0.9点）・G難度（1.1点）を足す。規則書に対応する条文は無く、本書だけが仕様。

| 項目 | 定義 | 備考 |
|------|------|------|
| 上限難度 | `FutureLevel`（`"F"` / `"G"` / `null`） | `null` がモードOFF＝現行規則（E止め） |
| 上限の選択肢 | `FUTURE_LEVELS` / `DEFAULT_FUTURE_LEVEL`（`"F"`） | UIのプルダウン（F難度まで／G難度まで） |
| 価値点 | `DIFF_SCORE.F = 0.9` / `DIFF_SCORE.G = 1.1` | E(0.7) から0.2刻み |
| 難度値 | `DIFF_VALUE.F = 6` / `DIFF_VALUE.G = 7` | 現行規則の上限は `MAX_DIFF = 5` のまま |
| 適用中の上限 | `maxDiff(future)` / `clampDifficulty(value, future)` | E止めだった箇所はすべてここを通す |
| F・G難度の技 | `Skill.future` が付いた `SKILL_LIST` の項目 | `futureSkillIds(future, junior, team)` で引ける |
| 団体でしか実施しない技 | `Skill.teamOnly` | 十年後モードの2回宙返り系。個人モードの選択肢に出さない |
| 十年後の難度認定 | `FUTURE_SKILL_DIFFICULTY` | 現行規則の技でも十年後は上がるもの（ルドルフ E→F） |
| 徒手系の数え方 | `HAND_MOTION_WEIGHT` / `handMotionValue` / `futureHandValue` | 縦回転を重く数えてF・Gを認定する（下表） |

### 十年後モードで増える技（`Skill.future`）

| 技 | id | 難度 | 個人/団体 |
|----|----|------|----------|
| 後方伸身宙返り4回ひねり | `f_backlay4twist` | F | 両方 |
| 後方伸身宙返り4回半ひねり | `f_backlay45twist` | F | 両方 |
| 後方伸身宙返り5回ひねり | `g_backlay5twist` | G | 両方 |
| 伸身前宙3回ひねり | `f_frontlay3` | F | 両方 |
| 伸身前宙4回ひねり | `g_frontlay4` | G | 両方 |
| 後方2回宙返り2回半ひねり（ルドルフハーフ） | `f_rudolphhalf` | F | 団体のみ |
| 後方伸身2回宙返り1回ひねり | `f_doublelay1twist` | F | 団体のみ |
| 後方2回宙返り3回ひねり（リジョンソン） | `g_rijonson` | G | 団体のみ |

### 徒手系のF・G（縦回転を重く数える）

投げ受けの間の徒手は、**縦回転（前転・後転・タッチダウンライズ・ギャンビ・徒手扱いの転回技）が
手具の滞空時間を食う**ので、横回転（シェネ・転がり）より重く数える。現行規則の「縦3動作＝E」
（＝4動作相当）と同じ考え方を上に伸ばしたもの。

- 重み（`HAND_MOTION_WEIGHT`）：横 1.0 ／ 縦 1.5
- しきい値（`FUTURE_HAND_STEPS`）：**5.0以上でF**、**5.5以上でG**

| 内容 | 重み換算 | 現行規則 | 十年後（上限G） |
|------|---------|---------|----------------|
| 4動作（横のみ） | 4.0 | E | E |
| 縦1＋横3 | 4.5 | E | E |
| 縦3動作（§3.5.5.3 の特例） | 4.5 | E | E |
| シェネ×5（5動作・横のみ） | 5.0 | E | **F** |
| 縦2＋横2 | 5.0 | E | **F** |
| シェネ×4→前転（5動作・縦横混在） | 5.5 | E | **G** |
| 縦3＋横1 | 5.5 | E | **G** |
| 前転×4（縦4動作） | 6.0 | E | **G** |
| 6動作 | 6.0〜 | E | **G** |

E以下は従来どおり**動作数だけ**で決めるので、現行規則の採点は変わらない。
**「縦3動作＝E」は上書きではなく最低保証**（`Math.max`）で、動作を足せばそのぶん上がる
（縦3動作＋シェネ3回＝6動作は十年後モードではG。以前は早期returnでEに固定され、
ただの6動作より低くなっていた）。

加えて、既存の**ルドルフ（`e_rudolph`、現行E）は十年後モードでF**として認定する
（`FUTURE_SKILL_DIFFICULTY`）。ひねり系はどれも §3.6.2 の刻みをそのまま伸ばした値と一致する
（`twistDiffValue()`）ので、一覧から選んでも手動入力（ひねり指定）で組んでも同じ難度・同じidになる。
**2回宙返り系は個人では実施しない**ので、十年後モードで増える2回宙返り系は `teamOnly` を付けて
団体モードの選択肢にだけ出す（既存の2回宙返り系は従来どおり両モードで選べる）。

- **難度計算の変更はE止めの上限だけ**。加点・減点・必須要素・重複判定・シリーズ構成のルールは現行のまま。
  - ひねりの難度（`twistDiffValue()`）：半ひねりごとに1段の刻みをそのまま伸ばし、
    後方は4回・4回半＝F／5回＝G、前方の伸身はそこから1段上（3回・3回半＝F／4回・4回半＝G）。
  - タンブリングの連続加算（`calcTumblingDifficulty`）：C→B→B→B ＝ F、もう1本足して G。
  - 徒手系難度（`calcHandDifficulty`）：動作数をA起点で足した値が上限まで伸びるほか、
    **縦回転を重く数えた動作量**でF・Gを認定する（下の「徒手系のF・G」）。
  - 手具操作加点 §3.5.5.5(3) は「E難度**以上**の転回系」で付く（現行規則では上限がEなので挙動は同じ）。
  - 団体も同じ上限を共有する（`TeamState.future` → `calcChunkDifficulty` / 5人格上げ / 交差の難度）。
- **技の選択肢**：`Skill.future` の技は、モードOFFでも上限を超える難度（上限FのときのG難度）でも出さない。
  `teamOnly` の技は個人モードに出さない（`skillAllowed(id, junior, future, team)` — 団体の画面だけ
  `team: true` を渡す）。ジュニアの2回宙返り禁止は十年後モードでも効く。選択済みの技を選択肢に残すときの
  但し書き（ジュニア禁止／十年後モード専用／団体のみ）は `skillBlockedReason()` が返す。
- **難度の丸め**：`skillDifficulty(id, junior, future)` が適用中の上限で丸めるので、**モードをOFFに戻しても
  F難度の技が0.9点で残ることはない**（E止めで採点される）。`skillDef()` が返すのは技そのものの定義なので
  丸めない（F・Gのまま）。
- **ランダム生成**：`GenerateOptions.future` を `autoTumblingTemplates` → `TransitionContext.future` まで
  引き渡す。F・G難度の技は**まだ誰も実施していない技**なので、「テンプレートに出てくる技だけで組む」制限の
  例外として候補に加える（`usableSkills` in `tumblingTransitions.ts`。2回宙返り系は現行どおり
  テンプレートに出てくるときだけで、`teamOnly` の技は個人の生成ではそもそも候補に入らない）。
  **モードOFFのときは候補の技に一切入らない** — 遷移表の技は `skillOptions(junior, flow, future)` から
  引くので、テンプレートにF難度の技が入っていても自動生成の連続には現れない。
  **投げシリーズ（投げタン以外）も5〜6動作の形を持つ**：`AUTO_THROW_PATTERNS` の
  `future` 付きの形（シェネ×5＝F／シェネ×6・シェネ×4→前転・前転×4＝G）を
  `throwPatternAllowed(pattern, future)` で出し分ける（現行規則では従来どおり4動作まで）。
  実測（スティック・各6本）：Dスコアの平均が OFF 4.9 → 上限F 5.5 → 上限G 6.2、
  投げ回数は5〜6回のまま変わらない。選ばれやすさは
  `SALTO_DIFFICULTY_WEIGHT`（D 0.6 → E 0.3 → F 0.15 → G 0.08）で、単発の高難度ほど出にくいのも同じ。
  連続の「難度は下がっていく」ルールも上限が上がるだけで変わらない
  （実測：上限なしで生成したときのDスコアが 現行4.9 → 上限F 5.5前後 → 上限G 6.0前後）。
- **UIは既定で隠す**：`IndividualScorer` / `TeamScorer` の「適用規則」カードに、ジュニアモードの下に
  トグルと上限プルダウンを出すが、**ジュニアモードを `FUTURE_UNLOCK_TOGGLES`（10）回切り替えるまで表示しない**。
  解放状態は端末ごとに localStorage（`mens-rg-scorer:future-unlock:v1`）に覚え、個人・団体で共有する
  （`useFutureUnlock` / `loadFutureUnlock` / `saveFutureUnlock`）。
- **保存**：`SaveData.future` / `TeamState.future`（任意・既定 null）としてエクスポート・共有URL・
  自動保存のドラフトに往復する（`normalizeFutureLevel()` が不正値を null に倒す）。

## 8.5 ジュニア適用規則（個人モード）

`§10 変更規則1`。個人モードのトグルスイッチ（`IndividualScorer` の「適用規則」カード）で ON/OFF する。
ON にすると `computeScore(series, apparatus, { junior: true })` が呼ばれ、以下が変わる。

| 項目 | 一般 | ジュニア | 実装 |
|------|------|---------|------|
| ダイビング前宙（`b_divefront`）の難度 | B | C | `JUNIOR_SKILL_DIFFICULTY` |
| 後方宙返り半ひねり（`b_backhalf`）の難度 | B | C | `JUNIOR_SKILL_DIFFICULTY` |
| 後方伸身宙返り半ひねり（`b_backlayhalf`）の難度 | B | C | `JUNIOR_SKILL_DIFFICULTY` |
| 投げ上げの最低回数 | 3（`THROW_COUNT_REQUIRED`） | 2（`JUNIOR_THROW_COUNT_REQUIRED`） | `throwCountRequired(junior)` |
| 投げ上げの上限回数 | なし | 5（`JUNIOR_THROW_COUNT_MAX`） | `throwCountMax(junior)` |
| 2回宙返り系 | 実施可 | 禁止（選択肢に出さない） | `Skill.isDoubleSalto` / `skillAllowed()` / `skillOptions(junior)` |
| バク転→後方伸身宙返りの連続（団体） | 連続加算どおり（B） | まとめてC | `JUNIOR_SKILL_COMBOS` / `juniorComboAt()` |
| 1シリーズの実施減点(E)（**団体のみ**） | 上限なし | 最大1.0（`JUNIOR_SERIES_EXECUTION_MAX`） | `clampSeriesExecution(v, junior)` |

2回宙返り系（後方2回宙返り・後方伸身2回宙返り・ダイビングダブル・ムーンサルト・ルドルフ）は
ジュニアでは選択肢から外す。すでに選ばれている構成を読み込んだ場合は、値を失わないよう
「（D・ジュニア禁止）」と印を付けて選択値として残す（採点自体は従来どおり行う）。

上限を超えた6回目以降の投げは**要素・難度・加点のどれにもカウントしない**（難度点の採用候補から外し、
投げ回数・投げ方/受け方の多様性・必須投げ・技術加点にも算入しない。
**シリーズ加点**（投げ2回以上の数え方）・**二つ投げ4動作加点**・**手具操作加点**（最高難度の判定）も
超過分は見ない）。そのうえで
**超過1回につき −0.30**（`THROW_COUNT_OVER_DEDUCTION`）。`ScoreResult` は実施回数を
`performedThrowCount`、要素として数えた回数を `totalThrowCount`、超過数を `overThrowCount` で返す。

- 難度参照は `skillDifficulty(id, junior, future)` に集約。`junior` は `computeScore` → `analyzeSeries` →
  `calcTumblingDifficulty` へ引き渡す（既定 `false` なので既存の呼び出しは無変更）。
  `skillDef()` を直接見ている `isSalto` / `category` / `isConnectA` は適用規則で変わらないため据え置き。
- `SaveData.junior`（任意・既定 false）としてファイル/テキスト/共有URLに往復する。
- **団体モードにもジュニアがある**：フラグは `TeamState.junior` に持ち（保存データに往復）、
  `computeTeamScore` が `analyzeTeamSeries` / `calcChunkDifficulty` / 加点へ引き渡す。
  技ごとの難度認定（`skillDifficulty(id, junior)`）と2回宙返り系の非表示は個人と同じ。
  加えて**連続技の認定**（`JUNIOR_SKILL_COMBOS` / `juniorComboAt()`）があり、バク転→後方伸身宙返りは
  まとめて1つのC難度として扱う。塊の中の**どこにあっても**認定するので、実際の実施どおり
  ロンダート→バク転→後方伸身宙返り（助走のA難度が前に付く形）でもCになる。まとめない場合の
  連続難度と**高い方**を採る（5人同時の格上げ後の値とも `max` を取るので、格上げが上回る構成では
  格上げ値が残る）。認定後のCは通常どおり連続加算の対象で、繰り返せば
  ロンダート→バク転→後方伸身→バク転→後方伸身 ＝ C+(C−1) ＝ E。
- **1シリーズの実施減点(E)の上限（1.0点）は団体のジュニアのみ**（`JUNIOR_SERIES_EXECUTION_MAX`）。
  `computeTeamScore` が `clampSeriesExecution()` を通して合算し、UIは `max` 属性と入力時の丸めの
  両方で上限を反映する。**個人は一般・ジュニアとも上限なし**、団体の演技全体の実施減点も上限なし。
- **未対応**：変更規則1-1〜1-2（手具1つのみ）。

### 8.6 テンプレートからのランダム生成（`generate.ts` ほか）

`generateRoutine(templates, opts)` は登録済みのシリーズテンプレートから演技構成を組み立てる。
中身は役割ごとに4ファイルに分かれている（`generate.ts` は入口で、すべてを再エクスポートするので
参照は今までどおり `from "./generate"` のまま）。

| ファイル | 受け持ち |
| --- | --- |
| `generateOptions.ts` | 入力と結果の型（`GenerateOptions` / `GenerateResult`）。`junior` / `future`（十年後モード）もここ |
| `generateWeights.ts` | **上限・目標値・重み**。`A_PRIORITY` / `*_WEIGHT` / `DEFAULT_MAX_*` / `preferredThrowCount` / `requiresAllElements` など、評価式に掛ける数だけ |
| `generateEvaluate.ts` | **構成の評価**。構成から数を取り出す関数（`hardThrowCount` / `otherStyleCount` / `saltoRepeatCount` …）と、それを足し引きする `evaluate` / `shortfallPenalty` |
| `generateSearch.ts` | **探索**。`greedyAttempt` / `swapIn` / `tuneAutoSeries` / `orderSeries` / `upgradeTumblings` / `satisfying` |
| `generate.ts` | 入口（`generateRoutine` / `generateForApparatus`）と再エクスポート |

評価式（`generateEvaluate.ts` の `evaluate`）はこの形をしている：

```
value = -(範囲外 + 本数超過) × 100        … 難度では覆せない
        - 満たせていない必須要素            … shortfallPenalty
        + Dスコア + 難度への上乗せ + A残点
        - 実在の好みに合わない形            … 投げの回数・その他の投げ受け・縦3動作 …
        - タイブレーク                      … 多様性・組み方の優先度・自動生成 …
```

重みの尺度は3段階で、**タイブレーク**（0.005〜0.05。難度の刻み0.1より小さいので同点のときだけ効く）／
**実在の好み**（0.1〜0.9。点数と引き換えになる）／**要求と範囲**（不足10・範囲外×100）に分かれる。
探索側はこの1つの数が上がるかどうかしか見ない。

- 使えるのは**指定した手具のテンプレート＋共通**（`usableTemplates()`）。手具を指定しない場合は
  `generateForApparatus()` が全手具で組んでいちばん良かったものを返す
- **必須要素を必ず満たすかは狙うDスコアで決まる**（`requiresAllElements`）。上限を指定しないか
  `REQUIRE_ALL_ELEMENTS_MIN_SCORE`（3.0）以上を狙うときは、不足1つにつき `REQUIRED_ELEMENT_WEIGHT`（10）を
  評価から引き、必ず満たしにいく。難度点（1本でも最大0.7）より十分大きく、範囲外ペナルティ（×100）よりは
  小さいので、**範囲に収めることが最優先、その中では要求を満たす構成**になる。
  それ未満（0〜1点台〜2点台）は従来どおりA減点（1つ −0.30）として扱うだけで、満たせなくてもよい
  - 根拠：必須要素をすべて満たす構成のDスコアは実測で2.0あたりが下限。3.0なら余裕があり、
    上限3.0で不足0が93%、3.5以上で100%になる（適用前は3.0で45%）
  - `requireAllElements` を渡せば、しきい値によらず明示的に切り替えられる
  - 生成結果の名前は種類だけ（自動生成の投げ／自動生成のタンブリング／自動生成の投げタン）。
    中身は隣に表示されるシリーズの内容で分かるので、名前に解説は付けない
  - 実測では、必須要素を必ず満たす設定にしても **D+A残点の平均はほぼ変わらない**
    （上限3.0で 12.05 vs 12.12、3.5で 12.71 vs 12.69、4.0で 13.24 vs 13.23）。
    つまり損失を比べるだけでも、たいていは同じ構成にたどり着く
- **満たせないときに何から諦めるかは現実の感覚の順**（`A_PRIORITY`、`shortfallPenalty`）：
  投げの回数 ＝ 各手具の必須投げ・受け ＞ 投げタン ＞ **D難度** ＞ 多様な投げ受け ＞ つなぎ ＞ 三宙
  ＞ つなぎの手具操作。D難度は `dScore` がそのまま担う。順位の重みは `A_PRIORITY_WEIGHT`（0.01）で
  難度点の刻み（0.1）より小さく、**同点のときのタイブレークにだけ効く**
  （規則どおりの減点額がすでにこの順序をおおむね表しているため）
- 評価関数は `-(Dスコアの範囲外分) * 100 - 満たせない要求（優先順位つき） + Dスコア + A残点`。
  **DとA残点は同じ重みで足す**ので、あるシリーズを入れるかどうかは
  「上がるD」と「増えるA減点」を比べて損失の少ないほうが選ばれる
  （手具操作なしのタンブリング −0.2、ジュニアの6回目の投げ −0.3、方向系・多様性の不足 など、
  `computeScore` が計算するA減点はすべて評価に入っている）。必須要素の不足もジュニアの投げ超過も
  A減点として効くので、この1つの値を最大化すれば「必須要素を満たしつつ難度を上げ、範囲に収める」になる
- **投げタンは1本まで**（`DEFAULT_MAX_THROW_TUMBLING`、`maxThrowTumbling` で変更可）。必須要素は1本で
  満たせるので、超えた分は範囲外と同じ強さのペナルティにして選ばれないようにする
- **単発で高難度な技ほど実施回数・頻度が少ない**。候補づくりの重み（`SALTO_DIFFICULTY_WEIGHT`：
  D＝0.6／E＝0.3）で抑えていて、上限は決めない。最大を狙う構成でも
  **D難度以上の単発は 0個18%／1個52%／2個25%／3個以上5%**（日本トップの実演技も2つ）。
  評価側の `HIGH_DIFFICULTY_WEIGHT`（0.02）は同点のときのタイブレークだけ
- **リングは単発高難度が更に少ない**（`APPARATUS_HIGH_DIFFICULTY_WEIGHT`：ring＝0.4）。
  リングは重く、持ったままひねるのが難しい。候補づくりの重みに掛け、評価側の
  `HIGH_DIFFICULTY_WEIGHT` は逆に割る（＝より強く嫌う）。技の一覧すべてから組んだ実測で
  **D難度以上の単発は 平均 スティック1.24個／クラブ0.96個／リング0.66個**（Dスコアはほぼ不変）
- **ハンドスプリング・転宙は実施が少ない**（`LIMITED_SKILLS`）。候補としては選ばれにくくし、
  **演技内で1回まで**（`LIMITED_SKILL_MAX`）。技として入れても徒手動作として入れても数える
  （`limitedSkillCounts`）。使うこと自体も `LIMITED_SKILL_WEIGHT`（0.02）だけ弱く嫌う
- **タンブリングは投げタンを含めて3本まで**（`DEFAULT_MAX_TUMBLINGS` ＝ `ADOPT_COUNT`、
  `maxTumblings` で変更可）。難度点に採用されるのは上位3本なので、4本目は評価されない。
  三宙・つなぎ技・投げタンを3本に収めるのは貪欲法だけだと当たり外れがあるため、
  必須要素を必ず満たす設定で最良の構成に不足が残ったときだけ、
  1本ずつ別の候補と入れ替えて詰める（`swapIn`。自動生成の本数の上限は守る）。
  それでも足りなければ、**不足を満たす候補を必ず入れた状態から組み直す**
  （`satisfying` → `greedyAttempt([t])` を `REBUILD_ATTEMPTS`＝5回まで、入れ替えも掛ける）。
  Dスコアの上限いっぱいの構成では1本足す代わりに1本抜く必要があり、入れ替えだけでは届かない
- **Dスコアは加点よりも難度点で上げる**（`DIFFICULTY_PREFERENCE_WEIGHT`＝0.3）。
  難度点（タンブリング＋徒手）1点あたり上乗せするので、**同じDスコアなら難度点で取っている
  構成**が選ばれる。上乗せは難度点0.1につき0.03＝難度点の刻み（0.1）より小さいので、
  Dスコアを下げてまで難度点を取ることはない。
  実測（40構成・上限なし）：難度点 3.90→3.93／加点 0.74→0.69（Dスコアは4.6台で変わらず）
- **タンブリングの難度点は徒手よりさらに優先する**（`TUMBLING_PREFERENCE_WEIGHT`＝0.3。
  タンブリング1点＝0.6、徒手1点＝0.3）。**上級者のタンブリングはほぼE難度**なので、
  加点や徒手で稼ぐ前にタンブリングの難度を上げる。
  ただし重みだけでは届かない：貪欲法はタンブリングの枠（`DEFAULT_MAX_TUMBLINGS`＝3本）が
  埋まったあとに出てきた高難度の候補を見られない（足すと本数超過で評価が下がる）ので、
  最後に**タンブリングだけを入れ替える**（`upgradeTumblings`。候補をタンブリングに絞った
  `swapIn` を `TUMBLING_UPGRADE_ROUNDS`＝2周。生成時間はほとんど増えない）。
  実測（20構成ずつ・タンブリングユニットのE難度率）：上限なし 67%→**100%**
  （タンブリングの難度点 1.89→2.10、D 4.60→4.81、加点は0.71のまま）／
  上限4.5点 62%→87%／上限2.5点 20%（変わらず。そのレベルの選手は実際にE難度を実施しない）
- **投げ上げの回数の最頻値はDスコアで決まる**（`preferredThrowCount` / `throwCountPenalty`）。
  Dスコアが上がるほど多くなり、**最小はルールの回数**（一般3回・ジュニア2回）。
  `THROW_COUNT_MODE_STEPS`：2点未満＝ルールの回数／2点台〜3点台＝4回／4点以上＝5回。
  最頻値より少ないぶんは `THROW_COUNT_UNDER_WEIGHT`（0.1）。多い側は技術加点に上限が無く
  投げを足すほど点が伸びてしまうので難度の刻み（0.1）より強い重みにするが、
  **1回多い構成は十分ありえる**（4点台でも6回を実施する）ので段を分ける：
  1回多いぶんは `THROW_COUNT_OVER_WEIGHT`（0.2）、2回以上多いぶんは
  `THROW_COUNT_FAR_OVER_WEIGHT`（0.3）。`THROW_COUNT_HIGH_SCORE`（5.0）以上を狙う構成では
  6回を実施する確率が上がる（最頻値は5のまま）ので、1回多い側は
  `THROW_COUNT_OVER_WEIGHT_HIGH`（0.15）と**弱める**。
  **本数の上限ではなくこの重みが回数を決める**（`DEFAULT_MAX_AUTO_THROWS` は最頻値の判断が
  効く範囲まで上げてある）
  実測（各20〜40構成）：D1.5→3回100%／D2.5→4回80%（3回13%・5回8%）／
  D3.5→4回60%・5回40%／D4.5→5回55%・6回40%・7回5%／上限なし→5回80%・6回20%
- **ルールの投げ回数はDスコアに関係なく必ず満たす**。`shortfallPenalty` の投げ回数だけは
  `mandatory` でなくても `REQUIRED_ELEMENT_WEIGHT` を足し、詰め直し（`swapIn`／組み直し）も
  投げ回数が足りなければ走る（`Evaluation.throwCountUnmet`）。Dスコアの上限が低い構成では
  難度を下げてでも投げ回数を満たす（0〜1点台の選手が満たさないのはタンブリング側の要求で、
  投げは投げるだけなので満たす）
- **難度を狙う投げは4回まで**（投げタン1回＋それ以外3回＝`ADOPT_COUNT` 本の徒手系ユニット）。
  それ以上の投げは**加点だけを狙う**ので徒手操作を足さない：難度に採用されない投げ受けに
  入っている操作を `extraThrowOperation` で数え、`EXTRA_THROW_OPERATION_WEIGHT`（0.01）だけ嫌う
- **連続投げは1回目のほうが難度が高い**のが普通（`reversedThrowOrderCount` ×
  `THROW_ORDER_WEIGHT`＝0.005）。2回目のほうが高い構成も現実にある（日本トップのロープの
  1シリーズ目は 手以外の投げ→キャッチ→視野外の投げ→シェネ→キャッチ）ので、
  禁止せず弱く嫌うだけにする。投げタンは転回系の難度で決まるので数えない
- **同じ宙返りの繰り返しは避ける**：演技全体で同じ宙返りを2回以上実施するごとに
  `SALTO_VARIETY_WEIGHT`（0.02）だけ評価を下げる（`saltoRepeatCount()`）。前宙（`REPEATABLE_SALTOS`）は
  何度実施しても数えない。難度点の最小単位0.1より小さい重みなので、**点数が上がるなら繰り返しも許す**
  （同点のときに多様な宙返りの構成が選ばれる程度の効き方）
- 手順は**ランダムな貪欲法＋調整＋刈り込み**を試行回数ぶん繰り返す：
  1. テンプレートをランダムな順に見て、評価が上がるものだけ足す
  2. 自動生成の投げのシェネの回数を、評価が上がるあいだ増減する（`tuneAutoThrows`）
  3. 抜いても評価が下がらないシリーズを取り除く（＝**評価されない要素を入れない**。
     4本目のタンブリング、ジュニアの6回目以降の投げ、重複するシリーズなどが自然に落ちる）
  4. **投げとタンブリングが交互になるように並べ替える**（`orderSeries`）。
     貪欲法は評価が上がった順に足すだけなので、そのままだと投げが先頭に固まる。
     転回系（宙返り・投げタン）を含むシリーズと投げ・徒手だけのシリーズに分けて均等に挟み込む。
     並びで点数は変わらないが、ジュニアの投げ上限は前から数えるので、
     評価が下がる並びになったときは元の順のままにする
- 結果は `series`／使ったテンプレート／`dScore`・`aScore`／満たせなかった必須要素のラベルを返す。
  UIは `GenerateModal`（手具・Dスコアの範囲を指定 → 生成 → 内容を確認して採点画面に反映）

候補は**登録テンプレートを先に見て、足りないところを自動生成で補う**。同点なら
`AUTO_SERIES_WEIGHT`（0.02）ぶん自動生成より登録テンプレートを優先する（多様性の重みと同じ考え方で、
点数を犠牲にはしない）。

**自動生成の割合はユーザーが指定できる**（`autoRatio`。`GenerateModal` のスライダー、0〜100%を25%刻み）。
シリーズ数の上限（`maxSeries`＝`DEFAULT_MAX_SERIES`＝8本）に対する割合を本数に換算し
（`autoSeriesMax`。1＝既定は種類ごとの上限だけで制限しない）、種類をまとめた本数の上限として
貪欲法の追加（`greedyAttempt`）と入れ替え（`withinAutoLimits`）の両方で守る。
0%なら候補そのものを作らず、**登録テンプレートだけで組む**。
実測（テンプレート3件・12構成ずつ）：0%→自動0本（3シリーズ・D0.80）／25%→2本（D2.51）／
50%→4本（D3.32）／75%・100%→5〜6本（D4.04。ここでは種類ごとの上限が先に効く）。

#### 技ごとの出やすさ（ユーザー設定。`skillWeights.ts`）

実測の重み（`SKILL_PICK_WEIGHT` など）は**書き換えない**。その上に載せる**倍率だけ**を
端末（`localStorage`。キーは `mens-rg-scorer:skill-weights:v1`）に持つ。
既定（1）から変えた技だけを保存するので：

- **すべて既定に戻す**＝保存した分を捨てるだけ（`resetSkillWeights`）
- **1技だけ戻す**＝そのidを捨てるだけ（`resetSkillWeight`。1に戻す操作でも消える）
- 壊れた保存データ（知らないid・数値でない・範囲外）は読み込み時に捨てる／丸める
  （`normalizeSkillWeights`。0〜3、0.1刻み）

`GenerateOptions.skillWeights` → `autoTumblingSpecs` → `TransitionContext` と渡り、
`baseSkillWeights` で掛かる。**位置ごとの重みを上書きする場所**
（`AFTER_BACK_LAYOUT_SALTOS` / `CONNECT_FINISH_RARE` / テンポの行）でも掛け直すので、
そこだけ設定が消えることはない。入りの技の抽選（`entryWeight`）にも効く。
**0 にした技は候補から外れる**（`usableSkills`）。画面は `SkillWeightModal`
（`GenerateModal` の「技ごとの出やすさ」から開く）。**採点には一切影響しない**。

#### 生成する形の珍しさ（`rarity`。ユーザーが選ぶ）

遷移表の辺が持つ重みは「どれくらい実施されるか」そのものなので、**重みを指数で変形**すれば
「ありふれた形だけ」〜「珍しい形を優先」を1つのつまみで動かせる。
`GenerateOptions.rarity`（0〜100、既定 `DEFAULT_RARITY`＝50）を `GenerateModal` のスライダーで選ぶ。

| つまみ | 指数（`rarityExponent`） | 挙動 |
| --- | --- | --- |
| 0 | `RARITY_COMMON_EXPONENT`＝3 | 重みの大きい形に尖る（最も遷移しやすい形だけ） |
| 50（既定） | 1 | 実測どおりの頻度 |
| 100 | `RARITY_RARE_EXPONENT`＝−1 | 重みを反転（珍しい形を優先） |

- 抽選（`pickWeighted` / `pickDifferent`）は指数を受け取る（`rarityWeight`。重み0は反転しても選ばれない）
- 0〜1の確率は `rarityChance(p, rarity)`＝`p^k /(p^k + (1-p)^k)`。0.5・0・1は動かず、
  それ以外は同じ向きに動く。**だから「珍しい並び」自体が動く**
  （技の最中の二つ投げ、きりもみの視野外投げ、つなぎを2本目の後に挟む形、実施例の無い形…）
- `cycler`（ひと回りするまで被らせない配り方）は対象外。候補を散らすための仕組みで、
  頻度を表しているわけではないため
- **評価（点数）は変えない**。点数が動く形は珍しさを上げても増えにくく、
  点数に中立な選択（どの宙返りか・どの受け方か）にそのまま効く

実測（手具3種×12構成ずつ）：

| 珍しさ | 実D | 使われた技の種類数 | 稀な技/構成 | その他の投げ受け/構成 |
| --- | --- | --- | --- | --- |
| 0 | 5.05 | 17 | **0.06** | 0.78 |
| 25 | 4.99 | 22 | 0.47 | 0.56 |
| 50 | 4.98 | 24 | 1.08 | 0.56 |
| 75 | 5.14 | 25 | 2.28 | 1.08 |
| 100 | 5.27 | 24 | **2.89** | 2.06 |

（稀な技＝ハンドスプリング・転宙・きりもみ・きりもみ転回・テンポひねり）

**その他の投げ受けは対象外**にしている（下記）。珍しい受け方ではなく
「種類を数えてもらう」ための入力なので、珍しさで増減させる意味がない。

- **その他の投げ受けは「種類を数えてもらう」ための入力**（珍しい受け方ではない）。
  規則の分類では同じ受け方に見えるが実態は違う、というものを**別の種類として数えてもらう**
  ための入力で、多様な投げ受け（`VARIETY_REQUIRED`＝3種類）を埋めるのが役割。
  生成もその想定で扱う（`otherStyleUsage`）：
  - **足りない種類を埋めている分は嫌わない**（役割を果たしている）
  - **余った分**は飾りなので `OTHER_STYLE_WEIGHT`（0.15＞技術加点0.1）で嫌い、
    さらに `trimSpareOtherStyles` が**タグ自体を外す**。タグを1つ外すとその投げは
    通常の投げになって種類が1つ増えるので、`OTHER_TRIM_PASSES`（4回）繰り返す。
    要求したDスコアの範囲から外れるときは外さない
  - **演技の最初の投げ／最初のキャッチには置かない**（`OTHER_FIRST_WEIGHT`＝1。
    「ほかと違う」ことを示す入力なので、比べる相手より先には置けない）
  - 珍しさ（`rarity`）の変形からも外す（頻度ではないので）

  実測（1構成あたり・珍しさ0/50/100）：その他のタグ 0.31/0.28/0.33、
  そのうち**余り0件・最初がその他0件**（対応前は 0.86/0.64/2.06 で余りが 20/13/5 件）。
  候補のその他率 9.3%/2.4%/0.7%、Dスコアは変わらず

#### 現実的だが実施例の無い形（`unseenShapes.ts`）

物理的には実施できるが競技での例が無い形（＝日本トップレベルなら実施するかもしれないが、
実際に見たことはない形）は、禁止せず**要求するDスコア（`minScore`）で出やすさを変える**。
宣言は `unseenShapes.ts` の `UNSEEN_SHAPES` **1か所だけ**で、この種の形を足す作業は
「表に1行」になる。

| 形 | id | 候補づくり |
| --- | --- | --- |
| 左手の視野外投げ | `leftHandNoViewThrow` | `AutoThrowStyle.rare`。スティックだけ（左手投げが必須投げの手具）。連続投げの1回目・2回目には使わない |
| 通常の投げタンの**背面キャッチ** | `throwTumBackCatch` | `TumblingDraws.backCatch`。視野外のキャッチ。連続投げが続く形（視野外で受けてそのまま投げられない）と2つ同時キャッチには付けない |
| 通常の投げタンの**左手投げ** | `throwTumLeftHandThrow` | `TumblingDraws.leftHandThrow`。スティックだけ。二つ投げとは同時に付かない |

宣言は4つ：

- `chance`（`UnseenChance`）— 候補に出す確率のカーブ。`unseenChance(要求値, curve)` が読む
- `earns` — その形が**稼ぐ点数**。評価はこれをそのまま打ち消す
- `extra` — 中立化のうえで、さらに嫌うぶん
- `count(series)` — 構成の中にその形がいくつあるか

`UNSEEN_CHANCE_DEFAULT` は `HARD_THROW_FREE_SCORE`（要求値が5.0を超えるまで抑える）の裏返しで、
**高いDを求められた構成でだけ点数のために手を出す**という形にしてある：

| 要求値 | 確率 |
| --- | --- |
| 指定なし〜4.5（`riseFrom`） | 3%（`base`） |
| 5.0 | 18% |
| 5.5 | 33% |
| 6.0以上 | 40%（`max`） |

**頻度を決める場所が2つある**のが、この仕組みの理由。抽選（候補に出す確率）だけでは
頻度を抑えられない：どれも技術加点（0.1）を稼ぐので、候補の3%が生成結果の55%になった
（貪欲法は点数が上がる候補を必ず採る）。かといって要求値で重みを動かすと、重みが加点を
下回る点で一気に跳ねる（0.165で0件、0.099で全部＝0.3点の差で1/20→13/20）。
そこで評価（`unseenPenalty`）は **`earns` をそのまま打ち消す**＝点数的に中立にし、
出現率の主導権を抽選に戻している。

`earns` で厳密に打ち消せるのは、稼ぎが技術加点（1タグ＝`TECHNIQUE_BONUS`、上限なし）の形だけ。
稼ぎが「手具別必須要素を満たす」側にある形（投げタンの左手投げ）は、必須要素の重み
（`REQUIRED_ELEMENT_WEIGHT`＝10）を打ち消すわけにはいかないので中立化できない。そこは
「同じ必須要素を満たす普通の形が候補に必ずある」ことで抑える（必須要素ぶんはどちらを選んでも
同額なので比較では相殺される）。その形は `earns`＝0・`extra`＝0.1 と宣言してある。

実測（スティック20構成あたりの左手の視野外投げ）：指定なし0 ／ 4.5で2 ／ 5.0で4 ／ 5.3で13。
投げタン側の2つは通して0〜3件。

#### 自動生成の投げシリーズ（`autoThrows.ts`）

投げ方（左手投げ・二つ投げ・視野外・手以外…）を網羅したテンプレートを手で登録しなくても
済むように、よくある投げシリーズはシステム側で組んで候補に足す。

- **形**（`AUTO_THROW_PATTERNS`）は7種類：
  - 投げ→1〜2回シェネ→前転→転がり→キャッチ
  - 投げ→1〜3回シェネ→前転→キャッチ（＋視野外の投げ→視野外のキャッチ）
  - 投げ→3〜4回シェネ→キャッチ（＋視野外の投げ→視野外のキャッチ）
  - 投げ→前転3回→キャッチ（シェネなし。前転は縦回転なので**縦3動作＝E難度**）
  - 投げ→シェネ→キャッチ／投げ→前転→キャッチ／投げ→キャッチ（**最低限の操作（徒手0〜1動作）で
    必須投げを満たす形**。スティックの「通常・視野外投げ→手以外のキャッチ」もここに入る。
    スティックの「1シェネキャッチ」、クラブ・リングの「二つ投げ→前転／シェネ→キャッチ」。
    難度が低いのでDスコアを抑えたいときに使われやすいが、上級者も普通に実施する）
  - 投げ→1シェネ→キャッチ→投げ→3〜4回シェネ→キャッチ／
    投げ→1シェネ→キャッチ→投げ→1〜3回シェネ→前転→キャッチ
    （**先に投げ受けを1本置く形**＝`leadPair`。操作を足さずに投げ方を1種類増やせるので、
    日本トップのロープの1シリーズ目も「手以外の投げ→キャッチ→視野外の投げ→シェネ→キャッチ」。
    先の投げ方は `leadThrowStyle` として配る（受けは通常のキャッチ）。
    二つ投げは2つ同時キャッチが要るので先の1本には使わない。
    連続投げは1回目の投げのほうが難度が高いことが多く、この形は1回目が低難度になるので、
    **1回目には1シェネを挟む**（`LEAD_THROW_CHENE_COUNT`＝1））
  - 投げ→3〜4回シェネ→キャッチ→投げ→キャッチ／投げ→1〜3回シェネ→前転→キャッチ→投げ→キャッチ
    （**あとに投げ受けを1本足す形**＝`trailPair`。**連続投げは1回目で難度を採ることが多い**ので、
    徒手は1回目に付き、2回目は徒手なしの投げ受けにする。2回目の投げ方は `trailThrowStyle`
    （手以外の投げは2回目には使わない）、受け方は `trailCatchStyle` を「徒手なしの投げ受け」
    （`MINIMAL_PATTERN`）として引くので、クラブの押さえつけ・ロープの足に絡めた受けも出る
    ＝**演技の締めにもなる**。1回目のキャッチはそこから投げに繋げる受け方だけ
    （`throwsAfterCatch`：視野外・手以外のキャッチを外す）。
    実測（80構成・連続投げ109本）：1回目が高難度 83%／2回目が高難度 17%
    （この形を入れる前は 66%／34%。`THROW_ORDER_WEIGHT` を上げても 71% までしか動かず、
    「1回目に難度を置く形」そのものが足りていなかった））
- **投げ方**（`autoThrowStyles`）はその手具で実施できるものすべて：通常の投げ／左手投げ（スティック）
  ／二つ投げ（クラブ・リング）／視野外／手以外／その他／手具を使った投げ（クラブ・リング）。
  **受け方**（`autoCatchStyles`）は通常／視野外／手以外／その他／手具で押さえつけてキャッチ
  （＝手具を使ったキャッチ。クラブ・リングのみ）、それに**2種類を同時に満たす受け方**
  （`COMBINED_CATCH_WEIGHT`＝0.1）：
  - **視野外＋手以外のキャッチ**（全手具。手以外の規則 `NON_HAND_CATCH_RULE` に従うので、
    スティック・クラブでは低難度の投げ＝徒手0〜1動作のときだけ）
  - **視野外＋手具を使ったキャッチ**（クラブ・リング。難度に関わらず起こりうる。
    クラブは実施例が無いので `NO_VIEW_USE_APPARATUS_WEIGHT`＝0.3 でさらに低く）
  技術加点はタグごとに付く（1回のキャッチで0.2）ので、候補としての重みで頻度を抑える。
  実測（15構成ずつ・キャッチ全体に対する割合）：スティック1%／クラブ3〜5%／リング7〜16%
- **その他の投げ・その他のキャッチは可能な限り使わない**。受け方は引く重みを
  `OTHER_CATCH_WEIGHT`（0.1）に下げ、投げ方は候補としては残したまま生成側の評価で
  `OTHER_STYLE_WEIGHT`（0.15／回。技術加点0.1より強い）だけ嫌う（`otherStyleCount`）。
  多様な投げ受け（必須要素＝重み10）を満たすのにどうしても必要なときだけ入る。
  実測（15構成ずつ）：重み無しでは構成の約半分がその他を1〜2回含んでいたが、
  スティックで15構成中1回・クラブ・リングでは0回になった
- 候補は「形 × 投げ方」を土台に作り、受け方・シェネの回数を**ひと回りするまで同じものを使わない順**
  （`cycler`）で配る。シェネは手の有無・手の種類で別の技として数える（Q&A Q28）ので、
  被らせないほど難度にも多様性にも効く。シェネのない形（前転3回）は手の種類を持たない
- **シェネの手**（`autoHandsVariants`＝手なし＋`HANDS_TYPES` の4種類）は実施の多さで重み付きに引く
  （`handsWeight`）：**手なしが最優先**で、片手上げ＝両手上げ ＞ その他 ＞ 回旋
  （`HANDS_PICK_WEIGHT`＝手なし1／片手・両手0.5／その他0.25／回旋0.1）。
  さらに**回旋は2動作までがメイン**なので、シェネの回数が多い形では回旋を引く重みを下げる
  （`cheneCountWeight`：2動作までは1、3動作は `SPIN_THIRD_CHENE_WEIGHT`＝0.3、
  4動作は `SPIN_OVER_CHENE_WEIGHT`＝0.1。ほかの手は回数で変わらない）。
  実測（スティック60回分）：手なし45%／両手22%／片手21%／その他10%／回旋3%、
  回旋のシェネ回数は1〜2動作79%・3動作以上21%
- **前転3回（縦3動作）自体が珍しい寄りの技**。候補に入れるかどうかを**1回だけ**引く
  （`VERTICAL_THREE_CHANCE`＝0.25。珍しさのつまみ `rarityChance` を通す）。
  形ごと・投げ方ごとに引くと生き残りが出て、結局貪欲法が拾ってしまうので**形ごと1回**。
  ただし縦3動作は徒手だけでE難度に届く唯一の形なので、**要求するDスコア**
  （`demandScore`＝`minScore`）が `VERTICAL_THREE_FREE_SCORE`（4.5）以上なら下げない
  （`HARD_THROW_FREE_SCORE` と同じ考え方）。
  実測（60構成）：下限4.0点で 11本 → **3本**、下限5.0点で 6本 → 7本（変わらず）、D平均は同じ
- **前転3回（縦3動作）は手具を使ったキャッチが主流**。`verticalThree` の形では受け方を
  重み付きで引き、手具で押さえつけて受ける以外は `VERTICAL_THREE_OTHER_CATCH_WEIGHT`（0.2）。
  さらに生成側で、手具を使ったキャッチ以外の縦3動作の投げ受けは
  `VERTICAL_THREE_THROW_WEIGHT`（0.85／本。この形が増やす点数より大きい）だけ嫌うので、
  **Dスコアの範囲を満たすのにどうしても必要なとき以外は入らない**
  （`verticalThreeThrowCount`。実測で上限なしは20構成中0〜1本、下限4.5点を指定すると
  10構成中9本＝点数を稼ぐうえで必要なときだけ実施する）
- **手以外のキャッチは手具ごとに実施しやすさが違う**（`NON_HAND_CATCH_RULE`）
  - スティック：**低難度の投げ**（徒手0〜1動作＝`NON_HAND_CATCH_MAX_MOTIONS`）で実施する
  - クラブ：低難度の投げで、しかも低確率（重み0.2）
  - リング・ロープ：徒手の多い投げでも普通に実施する
  - ロープは**足に絡めた手以外のキャッチで演技を締める**ことがとても多い（下の「演技の締め方」）
  - 実測（各25構成）：キャッチのうち手以外は スティック1%／クラブ1%／リング8%／ロープ6%。
    スティック・クラブは徒手0〜1動作のものだけ。ロープは手以外のキャッチを含む構成では
    ほぼ必ず最後に置かれる（25構成中7構成が手以外で終わる）
- **手以外の投げ・手具を使った投げのあとに多くの徒手やタンブリングは実施できない**
  （`hardThrowCount` / `HARD_THROW_TAGS`）。足や手具で投げた直後なので現実的なのは
  徒手 `HARD_THROW_MAX_MOTIONS`（1）動作までで、転回系を挟むこともまずない。
  Dスコアの**要求値（下限）**が `HARD_THROW_FREE_SCORE`（5.0）を超えるまでは
  `HARD_THROW_WEIGHT`（0.9／本。徒手系の最高難度0.7より大きい）だけ嫌う（`suppressHardThrow`）ので、
  **点数を稼ぐうえでどうしても必要なとき以外は出ない**。
  実測（20構成ずつ・2動作以上または転回系を挟んだ本数）：要求値なしで リング21本→4本／ロープ21本→0本
  （Dスコア平均 5.17→5.04／4.97→4.81）、下限4.7点では 8本（範囲を満たすために必要）、
  下限5.1点では嫌わないので29本のまま
- **演技の締め方は手具で決まっている**（`FINISH_CATCH_TAG`）
  - クラブ：最後に**もう一方の手具で押さえてキャッチ**し、そのまま演技を終える（`USE_APPARATUS_TAG`）
  - ロープ：最後に**足に絡めてキャッチ**し、そのまま演技を終える（`NON_HAND_TAG`）
  - スティック・リングには決まった締め方が無いので指定しない
  - `finishCatchLast` がその受けで終わるシリーズを構成の最後に置き（並びで点数は変わらない）、
    締めの形になるシリーズが1本も無いときは `FINISH_CATCH_WEIGHT`（0.05）ぶん嫌うことで1本入れさせる。
    重みは難度の刻み（0.1）より小さいので**難度を捨ててまで締めの形にはしない**
  - 実測（20構成ずつ）：並べ替えだけでクラブ19/20・ロープ18/20、重みを足してクラブ20/20・ロープ19/20
    （Dスコア平均は 4.32／4.13 のまま変わらない）
- **クラブ・リングは転がり・前転で終わってから手具を使ったキャッチ（押さえつけ）が定番**
  - 投げシリーズ：動作の最後が転がり・前転の形（`rollFinishShape`）では、押さえつけ以外の
    受け方を `ROLL_FINISH_OTHER_CATCH_WEIGHT`（0.2）にする
  - 投げタン：前転でつないだ着地を `ROLL_FINISH_PRESS_CATCH_CHANCE`（0.5）で押さえつけて受ける
    （手具が1つのスティック・ロープでは `stripForApparatus` が落とすので何も起きない）
  - 実測（各30構成）：転がり・前転で終わる投げ受けのうち押さえつけは クラブ48%／リング53%／
    スティック0%（どちらの手具でも最も多い受け方）
- **その他のキャッチから連続投げに続ける確率は低い**
  （`OTHER_CATCH_BEFORE_THROW_WEIGHT`＝0.2。視野外の投げ受けを足す形の直前の受け）
- **左手投げを視野外で受ける確率はかなり低い**（`LEFT_HAND_NO_VIEW_CATCH_WEIGHT`＝0.1。
  `catchStyleWeight` が受け方の抽選に掛ける）
- **連続投げで繋げない投げ方・受け方は作らない**
  - **どんな投げにも繋げられない受け方**（`NO_THROW_AFTER_CATCH_TAGS` / `canThrowAfterCatch`）：
    手以外のキャッチ（足や体で受けた手具はすぐには投げられない）、手具を使ったキャッチ
    （押さえつけた状態からは投げられない）。投げが続く形
    （`throwsAfterCatch`＝`noViewPair`・`trailPair`）の直前の受けから外す
    （`catchStylesForPattern`）。2種類を同時に満たす受け方も含むタグで判定する。
    投げタンのキャッチも、連続投げを続けるときは押さえつけない（`autoTumblings` の `pressCatch`）。
    実測（候補10周＋20構成ずつ）：「投げに繋げない受け→投げ」はクラブ 候補147件・生成13件、
    リング 候補145件・生成17件 → **すべて0件**
  - **同じ技術どうしだけ繋げられない**（`NO_SAME_TAG_AFTER_CATCH_TAGS`）：
    視野外で受けて**視野外に投げる**ことはできないが、視野外で受けて普通に見て投げるのは
    実施例がある（視野外投げ→1シェネ→視野外キャッチ→**二つ投げ→そのままキャッチ**）。
    だから受け方を引く時点では外さず、2回目の投げ方を引くときに外す
    （`trailThrowStylesAfter`。続けて視野外に投げる形 `noViewPair` では受け方の側で外す）。
    実測（候補40シード）：「視野外キャッチ→投げ」がスティック 178/1128・クラブ 226/1280 ・
    ロープ 164/880、生成20構成あたり5〜6件
  - 連続投げの2回目の手以外の投げ（ほぼ不可能）：先に投げ受けを1本置く形（`leadPair`）の
    本体の投げから手以外を外す（`throwStylesForPattern`）。先に置く1本目の投げは
    手以外でよい（日本トップのロープの1シリーズ目が 手以外の投げ→キャッチ→視野外の投げ→…）
  - **連続投げの2回目の二つ投げは、すぐ受ける形だけ**：`leadPair` の本体（＝2回目）から
    二つ投げを外す（`throwStylesForPattern`）。2回目の二つ投げで徒手を多く実施する
    （＝高難度にする）形は実施されない。実施例があるのは2回目の二つ投げをすぐ受ける形で、
    それは `trailPair`（1回目に徒手、そのあと徒手なしの投げ受け1本）が作る。
    実測：`leadPair` の本体が二つ投げ 80/400 → **0件**、`trailPair` の2回目は 105/480 のまま
  - 実施例の無い投げ方（`AutoThrowStyle.rare`）は連続投げの1回目・2回目に配らない
    （`nextLeadThrow` / `nextTrailThrow` / `secondThrowStyles`）。ラウンドロビンで配るので、
    残すと要求Dスコアによらず一定の割合で出てしまう
- 二つ投げは2つ同時キャッチ（`catchTwo`）で受けるので、`checkApparatusFlow` の警告が出ない。
  また**二つ投げには「手具で押さえつけてキャッチ」を付けない**（`catchStylesForThrow`）。
  手具が2つとも空中にあり、押さえつける手具が手元に無いため
- **シェネの回数は生成中に調整する**（`tuneAutoThrows`）。候補は組み立てた内容（`spec`）を持っているので、
  貪欲法で選んだあと・刈り込みの前に、各自動生成の投げのシェネ回数を**形ごとの範囲**（`cheneCountRange`）の
  中で増減し、評価が上がるなら採用する。Dスコアの**上限**を指定したときは「シリーズを丸ごと落とす」より先に
  「回数を減らして収める」が選ばれ、**下限**のときは回数を増やして届かせる
- **1構成に入れるのは `DEFAULT_MAX_AUTO_THROWS`（5本）まで**。難度を狙う投げは投げタン＋
  上位3本（`ADOPT_COUNT`）までだが、それを超える投げは**技術加点のために実施する**ので、
  本数そのものは投げ上げの回数の最頻値（`preferredThrowCount` / `throwCountPenalty`）に任せる。
  上限は「構成が自動生成の投げだけで埋まらない」ようにするためだけのもので、
  実測では5本と8本で結果が完全に一致する（＝最頻値の重みが先に効く）。
  実測（上限3本→5本、20構成ずつ）：D4.5→投げ5回90%・6回10% から 5回55%・6回40%・7回5%、
  Dスコア平均 4.08→4.18、技術加点平均 0.59→0.71（生成時間は1構成 364→381ms でほぼ変わらず）
- 候補にすぎないので、評価が上がらなければ使われない（**必ず入るわけではない**）。
  `autoThrows: false`（`GenerateModal` のチェックボックス）で完全に切れる
- テンプレートが1本も無いときは生成しない（投げだけの構成にはしない）。
  自動生成の投げは `SeriesTemplate` に `auto: true` を付けて返すだけで、localStorage には保存しない

#### 自動生成のタンブリング（`autoTumblings.ts` ほか）

技そのものではなく**組み合わせ**を自動化する。中身は役割ごとに5ファイルに分かれている。

| ファイル | 受け持ち |
| --- | --- |
| `tumblingPatterns.ts` | 組む**形**（宙返りの本数・つなぎの有無・投げ受けかどうか）。`AUTO_TUMBLING_PATTERNS` / `saltoCountRange` / `basicLevelPattern` |
| `tumblingChain.ts` | **連鎖のルール**（何に何が続けられるか・どこで終われるか）。`nextSaltoOptions` / `connectOptionsAfter` / `canEndChain` / `tumblingFlowErrors` |
| `tumblingWeights.ts` | **選ばれやすさと抽選の確率**（実際の演技での多さ）。`SKILL_PICK_WEIGHT` / `saltoWeights` / `rollAfterChance` / `backwardEndChance` |
| `tumblingTransitions.ts` | 上の2つから**導出した遷移表**。`buildTransitions(ctx)` |
| `autoTumblings.ts` | **候補の組み立て**（表を歩いて `AutoTumblingSpec` にし、`Series` に起こす）。従来の参照先を変えずに済むよう、上の4つを再エクスポートする |

**遷移表（`buildTransitions`）** は「直前の技 → 続けて実施できる技」を1本ずつの辺
（`SaltoEdge`）に畳んだもの。辺は `id` / `weight`（選ばれやすさ）に加えて、
`endNever`・`endNeedsBackwardDraw`・`endNeedsRareDraw`（そこで終われるか。抽選待ちなら印）、
`rollChance`（そのあと前転を付ける確率）、`throwRule`（`ok` / `never` / `kirimomiDraw`）を持つ。
**表は手書きせず、①②のルールから毎回導出する**（入力画面の制約は `skillOptions` に聞いたままなので、
プルダウンの絞り込みが変われば表も追随する）。「なぜこの技が生成されないのか」は表を1つ引けば分かる：
候補に出ていない（①②のルール）／重みが小さい／終われない（`endNever`・抽選待ち）／
投げられない位置（`throwRule`）。表の内容そのものは `__tests__/transitions.test.ts` が検証する
（シードを固定した生成結果ではなく、表を直接読むテスト）。

**1本ぶんの候補で1回だけ引く抽選**は `AutoTumblingSpec.draws`（`TumblingDraws`）にまとめてある
（`backwardEnd` / `rareEnd` / `layoutAfterConnect` / `backToForwardThrow` / `roll` /
`pressCatch` / `twoThrow` / `secondThrow`）。宙返りの本数を変える `withSaltoCount` が
**同じ判断を使い回す**ためで、終われる本数の判定は `chainEndsOk` 1か所に集約されている。

抽選のユーティリティ（`shuffled` / `cycler` / `pickWeighted` / `pickDifferent`）は `pick.ts` に共通化した
（`autoThrows.ts` / `autoTumblings.ts` / `generate.ts` に同じ実装が3つあったもの）。

判定そのものは2階建て。

**① 入力画面の制約**（`tumblingFlowErrors` で検算。判定は入力画面のプルダウンと同じ
`skillOptions(junior, skillFlowAfter(prev))` を使うので、系統の絞り込みが変わっても追随する）
  - 後方系はロンダート・バク転から入るか、後ろ向きに降りる宙返りに続けてのみ実施
    （`needsRoundoffBefore`。足りなければ入力画面と同じようにロンダートを補う）
  - ロンダート・バク転の直後は後方系だけ（`skillFlowAfter`）
  - ジュニアは2回宙返り系を実施しない

**② 実際の演技での組み方**（`nextSaltoOptions` / `connectOptionsAfter` / `saltoOptionsAfterConnect`）
  - 連続の次の系統は**直前の技が降りる向き**で決まる（`leadsBackward`）。
    後ろ向きなら後方系、前向きなら前方系・側方系
  - 連続の難度は**だんだん下がる**（例：後方1回半ひねり→前方1回ひねり→前宙、
    後方2回半ひねり→前宙→側宙）。**テンポ宙返り・テンポひねり**（`TEMPO_SKILLS`）だけは例外で、
    その後は難度が上がってよい
  - **後方系を続けて実施することは少ない**ので、テンポ以外の**後方系の宙返り**で連続は切る。
    ただし**前方の半ひねり**のように前方系から後ろ向きに降りた後は、そのまま後方系に入る
    （例：ロンダート→後方宙返り1回半ひねり→前宙半ひねり→ダイビング前宙 ＝ 三宙）。
    ただし**後方伸身宙返り（ひねりを含む）の後は 前宙・きりもみ・きりもみ転回**が主流なので、
    そこだけは連続が続く（`AFTER_BACK_LAYOUT_SALTOS`。側宙はその前宙に続けて実施する）。
    きりもみ・きりもみ転回は宙返りの連続の中でだけ宙返りとして数える技（Q&A Q7）で、
    まさにこの位置で実施するので難度の上下は問わない。実際の多さに合わせて
    **前宙＞きりもみ＞＞きりもみ転回**の重みで選ぶ（`saltoWeights`）
  - 後方伸身の後に**後方系（抱え込みの半ひねり系）を続ける選手もいる**
    （後方伸身2回ひねり→抱え込みの1回半ひねり）。主流ではないので
    `AFTER_BACK_LAYOUT_BACKWARD_SALTOS`（後方宙返り1回半ひねり・後方宙返り半ひねり、重み0.5）
    として低い頻度で残す。条件は2つ：**後ろ向きに降りる後方伸身（整数ひねり）の後だけ**
    （半ひねり系は前向きに降りるのでロンダートが要り、それはつなぎの形になる）と、
    連続の原則どおり**難度が上がらないこと**。抱え込みの半ひねり系は前向きに降りるので、
    そのまま前方系に続けて三宙にできる
    （実測：後方伸身の直後に技が続く366件のうち後方系は13件＝3.6%。
    例：ロンダート→後方伸身宙返り2回ひねり→後方宙返り1回半ひねり→前転）
  - **ダイビングの後は何も続かない**（`CHAIN_END_SKILLS`）。頭から着地する技なので、
    きりもみ・とび前転と同じ扱い。自動生成では組み立てない（`Skill.noAuto`）
  - **転宙の後は側宙だけ**（`ONLY_SIDE_SALTO_AFTER`）。ほかの宙返りを続けることも、
    つなぎ技を挟むことも、前転でつなぐこともない（前転は `NO_ROLL_AFTER_SKILLS` が落とす）。
    転宙はそこで終わるか側宙に続けるかのどちらかになる
  - **宙返りのあとのバク転はテンポの後だけ**（個人ではまず実施しない）。つなぎ技は
    前向きに降りた後のロンダート・側転・ハンドスプリング・とび前転
  - **つなぎの最後のただの後方宙返りは上級者は実施しない**（`CONNECT_FINISH_RARE`）。
    B難度がほしいときはダイビング前宙・後方伸身宙返り（後方伸身宙返りはそのまま前宙に続けられる）。
    ただしDスコアの低い選手・ジュニアは実施するので、**候補からは外さず選ばれにくくするだけ**にして、
    Dスコアが0〜1点台の構成（`basicLevel`）とジュニアでは重み付けをしない
  - **連続の最後にしか実施しない技**（`CHAIN_END_SKILLS`）の後には技を続けない。
    入りの技・つなぎ技にも使わない
    - とび前転・きりもみ：首から背中にかけて着地する
    - きりもみ転回：理屈のうえでは続けられるが、実際に続けた選手はいない
    - 側宙：連続の最後としてしか実施されない
  - **側転は徒手扱い**なので、宙返りの間に挟んでもつなぎ技にはならない（つなぎ技の選択肢に入れない）。
    側宙への入りの技としては使う
  - **個人で2回宙返り系を実施することはほぼない**ので、技の一覧からは組み立てない
    （テンプレートに出てくる＝実際に実施しているときだけ使う）。2回宙返りは後ろ向きに降りる
    テンポ以外の後方系なので、そこから連続を続けることもつなぎを挟むこともない
  - **投げタンは投げの後にロンダートを入れない**。手具の滞空時間の都合で1本目は前方系、
    そこから**前転**（`THROW_ROLL_MOTION`）でつなぐか、**側宙（たまに転宙）**（`THROW_FINISH_SALTOS`）を実施する
  - **投げタンの投げを二つ投げにする**形もある（`twoThrow` / `TWO_THROW_IN_TUMBLING_CHANCE`＝0.3。
    クラブ・リングで、**投げてから跳ぶ形だけ**＝`canTwoThrowTumbling`）。立って両方の手具を投げ、
    タンブリングして**2つ同時キャッチ**（`catchTwo`）で受けるので、必須投げ（二つ投げ）と
    転回系の投げ受けを1シリーズで両方満たせる。2つとも空中にあるので押さえつけては受けられず
    （`pressCatch` を落とす）、その間の技に手具操作も付かない（`applyApparatusOps` が投げタンには
    付けない＋入力画面も `handsEmptyFlags` で隠す）。宙返りの最中に両方を投げる形は作らない
    （実施されないし、必須投げは投げアイテムにしか付かない）。
    実測：候補の投げタンのうちクラブ23%・リング19%が二つ投げ（手具の流れ警告は0）。
    ただし必須投げは安い投げ受け1本でも満たせるので、**生成された構成に出るのは25構成中0〜6本**
  - **投げタンのキャッチのあとに連続投げを続ける**形もある（`secondThrow`）。
    確率は形ごとに違い、**投げてから宙返りを実施する形のほうが多い**
    （`PAIR_AFTER_THROW_FIRST_CHANCE`＝0.5／`PAIR_AFTER_THROW_IN_SKILL_CHANCE`＝0.2）。
    2回目の投げ方は `secondThrowStyles`（手以外の投げは2回目には使わない。スティックの
    左手投げ・クラブとリングの二つ投げは手元に戻っているので使える）
  - **狙うDスコアが上がるほど難しい技を実施する**（`SKILL_MAX_DIFF_STEPS` / `maxSkillDiffValue`）。
    単発の技の難度の上限を要求値（`targetScore`＝`maxScore`）で段にする：
    2点台までC、3点台までD、4.0点からは上限なし（上限を指定しなければ制限しない）。
    これが無いと**難度を上げずに本数だけ増やして**要求値を満たしてしまっていた。
    実測（手具3種×15構成ずつ）：

    | 上限 | 実D | 最大難度 前→後 | D以上の単発 前→後 | E難度の単発 前→後 |
    | --- | --- | --- | --- | --- |
    | 1.5 | 1.47 | C → C | 0.00 → 0.00 | 0.00 → 0.00 |
    | 2.0 | 1.96 | **E → C** | — → 0.00 | — → 0.00 |
    | 2.5 | 2.46 | **E → C** | — → 0.00 | — → 0.00 |
    | 3.0 | 2.96 | **E → D** | — → 0.38 | — → 0.00 |
    | 3.5 | 3.46 | **E → D** | — → 0.44 | — → 0.00 |
    | 4.5 | 4.40 | E → E | 0.89 → 0.76 | 0.36 → 0.18 |
    | なし | 4.96 | E → E | 0.98 → 0.78 | 0.27 → 0.09 |

    **転回技の平均難度は横ばいのままでよい**（2.27→2.57）。連続は難度が下がっていくので、
    どの水準でも大半の技はB・C難度になる。水準が出るのは「最も難しい単発」と「連続の本数」。
  - **表記より実際の難しさが一段上の技**（`HARDER_THAN_RATED`：転宙・きりもみ・きりもみ転回）。
    首や背中から着地する技・軸のずれる技は、規則上の難度が低くても実施の難しさが頭ひとつ抜けている。
    **難度点は規則どおりのまま**で、頻度の計算だけ1段上として扱う：
    - 選ばれやすさに `HARDER_THAN_RATED_WEIGHT`（0.6＝`SALTO_DIFFICULTY_WEIGHT` のC→Dの比）を掛ける。
      **位置ごとの重みの後に掛ける**ので、きりもみが実際に出てくる位置
      （`AFTER_BACK_LAYOUT_SALTOS`：前宙5＞きりもみ3＞きりもみ転回1）にも効く
    - 難度の上限（`SKILL_MAX_DIFF_STEPS`）も1段上で見るので、**2点台の構成にきりもみ転回は出ない**

    難度を引き直す（B→C）のではなく倍率にしたのは、`SALTO_DIFFICULTY_WEIGHT` がD難度以上しか
    区別しないため（B→Cでは何も変わらない）。
    実測（1構成あたり・上限2.5／4.5／なし）：
    転宙 0.22/0.22/0.24 → 0.09/0.24/0.27、きりもみ 0.18/0.31/0.42 → 0.11/0.29/0.29、
    きりもみ転回 0.31/0.42/0.51 → **0.00**/0.27/0.27。実Dは変わらず
  - **その時代の上限難度の単発は実戦でほぼ実施されない**（`TOP_SINGLE_WEIGHT`＝0.05。
    `saltoDifficultyWeight` が `maxDiff(future)` と同じ難度にだけ適用する）。
    現行規則ならE難度で、E難度のユニットは単発ではなく C→B→B の連続で作る
    （全国大会の最大Dスコアが5.0〜5.1という水準でも単発のEは稀。実施例自体はある）。
    単発と連続で点数は同じ0.7なので貪欲法はどちらでもよく、頻度は抽選で決まる。
    十年後モードでは上限が上がるので同じ扱いがF・Gに移り、Eは普通の高難度（0.3）に戻る。
    実測：E難度の単発／構成が 上限4.0〜5.0で 0.20〜0.42本 → **0.11〜0.18本**、実Dは変わらず
  - **前方系のあとのきりもみ転回**（`AFTER_FORWARD_KIRIMOMI`）。きりもみ系は宙返りの連続の中で
    だけ宙返りになる技（Q&A Q7）なので `saltoList` に入っておらず、後方伸身のあと
    （`AFTER_BACK_LAYOUT_SALTOS`）以外では出てこなかった。前宙→きりもみ転回は実施されるので
    （投げ→前宙→きりもみ転回→キャッチ）、前方系のあとに限って足す。
    **難度の上限は掛けない**（きりもみ転回のC難度は「連続が1段上がった」という意味ではない）。
    きりもみは入れない：首から背中にかけて着地するので、そのまま受けには繋げられない。
    投げてから跳ぶ投げタンの2本目（`THROW_FINISH_SALTOS`）にも入れた。
    選ばれやすさは `SKILL_PICK_WEIGHT`（0.3）がそのまま効くので 側宙（1）＞きりもみ転回＞転宙（0.2）。
    実測：投げタンの2本目が 側宙74%・きりもみ転回17%・転宙10%（候補800件）、
    通常のタンブリング候補では 74/1279（5.8%）が含む。生成20構成のD平均は
    4.80→4.81（スティック）・4.93→5.05（クラブ）
  - **ひねりのある前方系の最中に投げるのはかなり難しい**（`THROW_IN_TWIST_SALTO_WEIGHT`＝0.3）。
    実施例のある形は**ひねりの無い前宙**で投げるもの（ロンダート→後方伸身2回半ひねり→
    前宙(投げ)→前転→キャッチ）。実施例が無い側宙・きりもみ転回（0.1）より上に置く。
    技を1つずつ並べず `Skill.twist` で判定するのは、前方宙返り1回ひねりだけ下げると
    伸身前宙1回ひねりが繰り上がるため。
    実測（技の最中の投げ）：前方宙返り1回ひねり 8/112 → **2/116**、伸身前宙1回ひねりは1〜2のまま。
    ただし**減るのではなく配り直されている**（側宙 9%→16%、転宙 8%→10%）。
    投げる技は連続の最後の技で、`pickDifferent` が既に使った前宙を繰り返さないため。
    形そのものを減らしたいなら、最後の技を引き直すのではなく候補を落とす作りにする
  - **「その技の実施中に投げるのが稀」の表は漏らさない**（`THROW_IN_SALTO_WEIGHT`）。
    側宙だけを 0.1 に下げていたので、きりもみ転回を前方系の続きに足した時点で
    **連続の最後で投げる形（`chainThrowInSkill`）の締めが きりもみ転回21% ＞ 側宙8%** に逆転した。
    きりもみ転回も同じ 0.1 にして解消（きりもみ転回はその形の上位6件から消え、側宙は9%に戻った）。
    生成160構成での連続の締め：側宙98 / きりもみ転回56 / 転宙31。
    **この比に対して抽選の効きは弱い**（きりもみ転回の重みを 0.3→0.15 にしても 57%→49%、
    0.02 でも33件残る）。貪欲法は「別の締め」が候補にあれば採るので、
    さらに下げたいなら抽選ではなく評価の重み（`unseenShapes.ts` と同じ考え方）にする
  - **つなぎの後の宙返りで投げる形**（`connectThrowInSkill`）。つなぎ技を挟んでから、
    その後の宙返りの最中に投げて前転→キャッチ。投げる技は**大抵ダイビング前宙か前宙**なので
    `THROW_AFTER_CONNECT_SALTOS` × `THROW_AFTER_CONNECT_WEIGHT`（5）で強く引く。
    つなぎを挟む位置は `count - 1`（＝投げるのがつなぎの直後になる）。
    つなぎ技を挟むと**向きが変わる**ので、投げられる位置の判定（`chainEndsOk`）と
    視野外タグ（`throwInSkillTypes`）は直前の宙返りではなく**並びの直前の技**を見る
    （見ないと ロンダート→…→ロンダート→前宙(投げ) が「切り返しの投げ」として落ちる）。
    実測（候補439件）：投げる技はダイビング前宙60%・前宙15%、
    生成20構成中 スティック1件・クラブ7件がこの形を含む
  - **加点を狙わないタンブリングの手具操作は最低限**（`applyApparatusOps`）。
    減点を避けるのに必要なのは「最後の宙返り1本」と「つなぎ技のA難度」だけなので、
    そこにしか付けない。手具操作加点（§3.5.5.5(3)）が狙えるE難度のときだけ宙返り2本に付け、
    投げタンは手具操作なしでも減点されない（A減点は投げの無いシリーズだけを見る）ので
    最低限＝0本（技の最中に投げてE難度になるときだけ、その技に付けて加点を狙う）。
    実測：技のうち手具操作ありは46%（従来は100%）で、手具操作なし減点は0、
    手具操作加点は1構成につき0.1のまま
  - **きりもみ系には手具操作を付けない**（`canOperateApparatus` / `Skill.noApparatusOp`）。
    きりもみ・きりもみ転回は首から背中にかけて着地するので、実施中に手具を操作できない。
    採点側も入力に残っている手具操作を数えない（`analyzeSeries` のユニット・
    手具操作加点の操作回数・手具操作なし減点のすべて）。入力画面ではチェックを出さない。
    技の最中の投げ（手を離すだけ）は実施できるので、そちらは従来どおり
    （きりもみの視野外投げ＝`BACK_TO_FORWARD_THROW_CHANCE`）。
    実測（候補60シード）：きりもみ系に手具操作ありが 76/82 → **0/88**、
    生成20構成のD平均 4.88→4.80（スティック）・5.08→4.93（クラブ）、
    手具操作なし減点は0のまま・手具操作加点も1構成0.1のまま
  - **ロンダートから入る**ことを優先する（1本目に後方系の宙返りを選ぶ重みを上げると、
    入りの技がロンダートになる）。宙返りの途中で投げる投げタン（`throwInSkill`）は
    狙うDスコアに関わらず `THROW_IN_SKILL_ROUNDOFF_WEIGHT`（3）で優先し、
    通常のタンブリングも `roundoffEntryWeight` で優先するが、**Dスコアが上がるほど頻度を下げる**
    （`ROUNDOFF_ENTRY_WEIGHT`＝3 に1点ごと `ROUNDOFF_ENTRY_DECAY`＝0.7 を掛け、1未満にはしない。
    上限を指定しない＝難度を狙いきる構成では優先しない）。
    低いDスコアでつなぎ技を満たす形（ロンダート→宙返り→つなぎ→宙返り）はいちばん多い形なので、
    `ROUNDOFF_ENTRY_CONNECT_MAX_SCORE`（3.0）未満のつなぎの形では `ROUNDOFF_ENTRY_CONNECT_BOOST`（2）
    でさらに優先する。実測（候補の1本目が後方系＝ロンダート入りの割合）：
    目標1.5点80%／2.0点80%／2.5点77%／3.0点以上69〜70%、つなぎの形は低スコアで83%（上限なしは69%）。
    実測：候補87%／生成された投げタン71%がロンダート入り（`throwInSkill`）
  - **前方系から入る通常のタンブリング**（ロンダートを使わず助走から前方系、または
    ハンドスプリング入り）は、Dスコア0〜1点台の初心者でしか見かけない。
    `forwardEntryWeight` が 1本目の前方系の重みに `FORWARD_ENTRY_WEIGHT`（0.2）を掛ける。
    目標Dスコアが `FORWARD_ENTRY_FREE_SCORE`（2.0）以下なら下げず、上限なし（＝難度を狙いきる）は下げる。
    **つなぎの形（前宙→ロンダート→後方系）は中級者も実施する**ので、
    `FORWARD_ENTRY_CONNECT_FREE_SCORE`（3.5）までは下げない。投げタンは対象外
    （投げてから跳ぶ形は手具の滞空時間の都合で前方系しか実施できない）。
    上の `roundoffEntryWeight` の減衰とは役割が違う：あちらは「低スコアでは組み方の幅が狭く
    ロンダート入りがほぼ唯一」という**上乗せ**の減衰で、高スコアで前方系入りを増やす意味ではない。
    実測（自動生成のタンブリングが前方系入りになる割合。目標3.0／4.5／上限なし）：
    珍しさ50で 30%／28%／32% → **12%／8%／10%**、珍しさ0で 14%／22%／22% → **14%／0%／6%**。
    目標1.5点は変わらない（初心者の形は残る）
  - **切り返しからの側宙**（後ろ向きで終わる宙返り→前方系→側宙）は少し珍しい寄りなので、
    その位置の側宙だけ `SWITCH_SIDE_SALTO_WEIGHT`（0.3）で下げる。
    そのために遷移表の `next(prevId, beforePrevId?)` が**その前の技**を取る。
    `autoTumblings` は**つなぎ技を含んだ並び**（`flow`）を持って渡す：つなぎを挟むと向きが変わるので、
    そこを切り返しと読んではいけない（`rollAfterChance` が並びから直前の技を取るのと同じ理由）。
    実測（側宙の続きのうち切り返しからのもの）：目標3.0点 9/26 → 3/22、上限なし 10/31 → 3/25
  - **つなぎのあとの宙返りにハーフ（後方の半ひねり）を使う**ことは、つなぎの**前**の宙返りで
    使うことに比べて少ないので、`afterConnect` の辺で `CONNECT_FINISH_HALF_WEIGHT`（0.3）を掛ける。
    技を並べず `twist`（`isBackHalfTwistSalto`）で判定するのは、1つ下げると隣が繰り上がるのを防ぐため。
    基本的な構成（`basicLevel`）は対象外。
    実測（つなぎの前/後でハーフを使った回数）：上限なし 7/11 → 6/2、目標3.0点 8/5 → 6/1
  - **後ろ向きで終わる宙返り→前方系の宙返り**の位置で投げた例は無いので基本作らない
    （`isBackToForwardThrow`）。ただし**きりもみで視野外に投げる**形だけは物理的にあり得て
    見栄えも悪くないので、`BACK_TO_FORWARD_THROW_CHANCE`（0.15）の確率で残し、
    そのときは視野外投げのタグを付ける（`throwInSkillTypes`）。
    半ひねり系の後方宙返り（後方伸身1回半ひねり等）は前向きに降りるのでこの判定の対象外
  - ただし**連続の最後の宙返りの最中に投げる**形（`throwInSkill`）は別で、投げる前は普通の
    タンブリングなので入りのロンダートも本数も自由。**三宙と投げタンを1シリーズで両立**できるので
    日本トップも実施する（クラブ：ロンダート→後方伸身2回半ひねり→前宙（投げ）→前転→キャッチ）
  - 同じ技の繰り返しは避ける（`pickDifferent`。他に使える技が無ければ許す）
  - **難度が上がる例外**（`DIFFICULTY_RISE_AFTER`）：後方系のあとにそれより難度の高い
    前方系を実施する機会は少ないが、**後方宙返り半ひねり→前方宙返り1回ひねり**
    （さらにそこから前宙）を実施する選手はいる。機会は少ないので選ばれやすさも下げる
    （`DIFFICULTY_RISE_WEIGHT`＝0.3）
  - **ジュニアで低いDスコアを狙うときは、格上げされる技を優先する**
    （`withJuniorBoost`：`JUNIOR_SKILL_DIFFICULTY` の ダイビング前宙・後方宙返り半ひねり・
    後方伸身宙返り半ひねり を `JUNIOR_UPGRADE_BOOST`＝2倍。狙うDスコアが
    `JUNIOR_UPGRADE_BOOST_MAX_SCORE`＝3.0未満のときだけ）
  - **難度が同じ技でも実施の多さは違う**（`SKILL_PICK_WEIGHT`。既定1に対する重み）：
    ロンダート（1）＞ バク転（0.5）＞ ハンドスプリング（0.5。`LIMITED_SKILLS` の
    `RARE_PICK_WEIGHT`＝0.2 も掛かるので**実質0.1**）（入りの技・つなぎ技）／
    前方宙返り1回ひねり ＞ 伸身前宙 ＞ きりもみ転回／側宙 ＞ 転宙／
    抱え込み＝伸身 ＞ 屈伸／前宙 ＞ 前宙半ひねり／
    テンポひねりは後方系のC難度のなかで最も少ない（ただし屈伸より上）
  - **テンポひねりの次は テンポ宙返り ＞ それ以外の宙返り ＞ バク転**
    （`AFTER_TEMPO_TWIST_WEIGHT`＝3／`TEMPO_CONNECT_WEIGHT`＝0.3。
    つなぎ技がバク転になるのはテンポ系の後だけなので、テンポ系を1本目にする
    つなぎの形自体を選ばれにくくする）
  - **後方宙返り半ひねりで終わることは稀**（`RARE_CHAIN_END_SKILLS` / `RARE_CHAIN_END_CHANCE`＝0.1）。
    前向きに降りるのでそのまま終われてしまうが、上級者は大抵そのあとに前宙か側宙を実施する。
    終わりの本数を決めるときに**1本伸ばして**前宙・側宙に続け、続けられなければその候補は作らない。
    Dスコアの低い選手（`basicLevel`）はそのまま終わってよい。
    実測（上限なし・タンブリング75本）：最後の技は 側宙24／前宙18／…／後方宙返り半ひねり0
  - **つなぎ技は1本目の後だけでなく2本目の後にも入る**（`connectAt`。既定は `DEFAULT_CONNECT_AT`＝1、
    2本目の後は `CONNECT_AT_SECOND_CHANCE`＝0.4 の抽選で、宙返りを3本取れる候補だけ）。
    「前向きで終わる後方系→前宙→つなぎ→宙返り」はよくあるシリーズなので、この位置も作る。
    つなぎで終わる形は作らない（`endsOk` は `n > connectAt` を要求し、`withSaltoCount` も
    つなぎの後の1本を残す）。実測：つなぎ候補38本のうち2本目の後が5本（入力制約エラーは0）、
    生成20構成に2本（例：ロンダート→後方伸身宙返り半ひねり→前宙→ロンダート→後方宙返り1回半ひねり）
  - **つなぎのあとに後方伸身系を1本だけ実施して終わる形は稀**
    （`layoutOnlyAfterConnect` / `LAYOUT_AFTER_CONNECT_CHANCE`＝0.1）。
    2回半ひねり→ロンダート→後方伸身1回半ひねり で終わるような形は上級者ではあまり実施しない。
    あり得ない並びではないので抽選で少しだけ許し、それ以外は**もう1本（前方系）続ける**
    （後方伸身の後は `AFTER_BACK_LAYOUT_SALTOS`＝前宙・きりもみ・きりもみ転回）。
    Dスコアの低い選手（`basicLevel`）はそのまま終わってよい。
    実測（つなぎ候補46本）：抽選なしなら3本 → 0本
  - **前方系で終わったあとは大抵そのまま前転をする**（`rollAfterChance(id, prevId)`）。
    前宙だけは前転のありなし両方あるので半々（`ROLL_AFTER_FRONT_CHANCE`＝0.5）、
    それ以外の前方系（前方1回ひねり・伸身前宙など）はほぼ必ず前転をする
    （`ROLL_AFTER_FORWARD_CHANCE`＝0.9）。**きりもみ・きりもみ転回・とび前転の後は何もしない**
    （首から背中に着地する `CHAIN_END_SKILLS`）、側宙・後ろ向きで終わる技の後も前転をしない（`noRollAfter`）。
    **前向きで終わる後方宙返り**（半ひねり系・ダイビング前宙。つなぎの後半に抱え込みを実施した形もこれ）は
    前転・そのまま終了・前宙 どれも普通にあるので半々（`ROLL_AFTER_BACK_FORWARD_LANDING_CHANCE`＝0.5）。
    **切り返し**（後ろ向きで終わる宙返り→前方系）のあとは前転をしないことが多いので低く
    （`ROLL_AFTER_SWITCH_CHANCE`＝0.2。直前の技は並びから取るので、つなぎ技を挟んで向きが変われば通常どおり）。
    実測（候補・15周）：切り返しの前方系 13%／前向きで終わる後方宙返り 48%／前宙 50%／
    前宙以外の前方系 73%／側宙・きりもみ系 0%
    前転は徒手動作として足すだけなので**点数は変わらない**（タンブリングユニットの難度は
    転回技だけで決まる。実測：ロンダート→後方1回半→前方1回ひねり は前転のありなしとも D 0.80）。
    実測（候補）：前宙で終わる33本中16本、前宙以外の前方系で終わる6本中5本に前転が付く
  - **側宙の後に前転は実施しない**（`NO_ROLL_AFTER_SKILLS` / `noRollAfter`）。
    物理的に破綻はしていないが実際には無いので、側宙で投げる形はそのまま受ける
  - **連続を終える技の後に技・徒手動作が続く並びは警告を出す**（`tumblingChainEndErrors`）。
    自動生成は作らない（候補・生成結果とも実測0件）が、手入力・インポート・古い保存データでは
    起こりうるので、入力画面でも手具の流れの警告と並べて出す（採点には影響しない）。
    キャッチは続けてよい（投げ→前宙→きりもみ転回→キャッチ がその形）。
    `tumblingFlowErrors` にも入れたので、自動生成の自己検算にも乗る
  - **首・背中から着地する技の後にも前転は実施しない**（`noRollAfter` が `endsChain` も見る）。
    とび前転・きりもみ・きりもみ転回・ダイビングは連続を終える技（`CHAIN_END_SKILLS`）で、
    その後に技を続けられないのだから前転も続けられない。
    連続の途中に付ける前転（`rollAfterChance`）は以前から0だったが、**投げタンの締めの前転**
    （`rollFinish`）だけが `NO_ROLL_AFTER_SKILLS`（側宙・転宙）しか見ておらず、
    `…→きりもみ→前転→キャッチ` が生成されていた。
    実測（候補60シード）：連続を終える技の後の徒手が スティック17/447・リング20/465 → **すべて0**
  - **後ろ向きで終わる技**（`endsFacingBackward`：整数ひねりの後方宙返り／半ひねり・
    1回半ひねりの前方宙返り）の後は
    - **何も実施せず終わることはない**（`canEndChain`）。連続の最後にはしない
    - **前転も実施しない**（`noRollAfter`）
    - 後方宙返りで終わるのは**Dスコアが低い選手なら実施する**ので、狙うDスコアで
      決まる確率で抽選する（`backwardEndChance`：1点ごとに半分の指数的な減り方で、
      複数のシリーズで連続技を検討するレベル＝`BACKWARD_END_ZERO_SCORE`＝3.0点で0。
      上限を指定しない構成も0）。実測：1.5点→8%／2点→10%／2.5点以上→0%
    - **前方の半ひねり・1回半ひねりで終わるのは実戦でほぼ無い**ので、抽選に関係なく作らない
    - 2回宙返り系は連続も繋ぎもせずそこで終わる技なので例外
  - **側宙の実施中に投げる構成は稀**（`THROW_IN_SIDE_SALTO_WEIGHT`＝0.1）。
    クラブでの練習動画はあるが実戦で使われた記録は無いので、連続の最後で投げる形
    （`throwInSkill`）で側宙を引く確率を下げる（実測でタンブリング90本中3本）
  - **バク転は入りの技には使わない**。入りに足しても難度も要求も変わらない
    （つなぎ技として数えるのは宙返り−A難度−宙返りの並びだけ）ので、
    合理的な理由が無ければ実施しない（`TUMBLING_ENTRIES` の後方系はロンダートだけ）
    （ハンドスプリング・転宙は `LIMITED_SKILLS` で 0.2 なのでここには書かない）
  - **ハンドスプリングの入りはとくに少ない**。入りの技は「ひと回りするまで同じものを使わない」
    順（`pickDifferent`）で配るが、そこに実施が少ない技（`LIMITED_SKILLS`）を混ぜると
    **必ず1本は出てしまう**ので、使用済み扱いにして重み（実質0.1）だけで引く。
    実測（候補430本・20構成ずつ）：入り 7%→0.5%／つなぎ 4%→1%、
    ハンドスプリングを含む構成は 25〜45% → 0〜15%
  - **つなぎのあとに難度が上がる組み方は少ない**（`CONNECT_RISE_WEIGHT`＝0.3）。
    つなぎで勢いは作り直せるが、E難度は C→B→B ＞ B→C→B

**形**（`AUTO_TUMBLING_PATTERNS`）：宙返りの連続（1〜3本。3本で三宙）／つなぎ技入り／
投げ受け＋前転／投げ受け＋側宙／連続（2〜3本）の最後の宙返りの最中に投げて前転→キャッチ。

**同じ難度に到達する組み方の優先度**（`TUMBLING_SHAPE_ORDER` / `readTumblingShape` /
`tumblingShapeRank` / `throwTumblingShapeRank`）。難度点が同じ組み方のあいだで、実際の演技での
多さの順位を付ける。評価では順位1つにつき `SHAPE_PRIORITY_WEIGHT`（0.005）だけ下げるので、
**同じ点数ならより実施される組み方**が選ばれるだけ（順位は最大5、タンブリング3本で0.075＜0.1）。

- タンブリング（つなぎのA難度は数えない難度の並び）
  - E難度：C→B→B ＞ D→B ＝ C→C ＞ C→C→B ＞ B→B→B→B ＞ その他 ＞ 単発E
  - D難度：C→B ＝ B→B→B ＞ その他 ＞ 単発D
- 投げタン
  - E難度：C＋側宙 ＝ D＋前転 ＞ つなぎを挟んで最後のB難度で投げ ＞ 連続の最後で投げ
    ＞ C＋側宙以外の宙返り ＞ その他 ＞ 単発E
  - D難度：前方のC＋前転 ＝ 前方のB＋側宙（転宙）＞ C難度のシリーズ中に投げ ＞ その他

**Dスコアが0〜1点台の選手**（`BASIC_LEVEL_MAX_SCORE`＝2.0 未満を狙う構成 → `basicLevel`）は、
ルールの要求を満たしきれない単純なタンブリングを実施するので、候補もそこに寄せる。

- ロンダート→何かしらの宙返りを実施して終わり
- 三宙なし（連続は `BASIC_LEVEL_MAX_SALTOS`＝2本まで）
- つなぎなし（つなぎの形を作らない）
- D難度なし（`BASIC_LEVEL_MAX_DIFF`＝C難度まで）
- ただの後方宙返りのような基本技も普通に実施する（重み付けなし）

※ 手具操作は付けたままにしている（実際にはこの層は手具操作なしのことが多いが、
入れておいたほうが減点の確認に使いやすく、外すのはチェックを外すだけで済むため）。
投げタンで `SkillItem.isThrow`（技の最中に投げ）を使うのは `throwInSkill` の形だけで、
**投げるのは連続の最後の宙返り**（1シリーズに1つ）。

- **使う技は登録テンプレートに出てくる技だけ**（`usedSkillIds` → `skillIds`）。
  その選手が実際に実施している技の**組み合わせ**だけを自動で作る。使える技が無い形は作らない。
  テンプレートが1つも無いときだけ技の一覧から自由に組む（＝テンプレート0件でも生成できる）
- 宙返りの本数は投げのシェネと同じく `tuneAutoSeries` で増減し、Dスコアの範囲に合わせる
- 1構成に入れるのは `DEFAULT_MAX_AUTO_TUMBLINGS`（4本）まで。難度に採用されるのは上位3本だが、
  4本目が必須要素（三宙・つなぎ技・方向系）を担うことがあるので1本ぶん余裕を持たせる
- 手具操作（`hasApparatus`）はすべての技に付ける（手具操作なしの減点・つなぎ技の −0.2 を受けないため）

---

## 9. UI構成

| コンポーネント | 役割 |
|---------------|------|
| `App.tsx` | モード切替（個人/団体）のシェル |
| `IndividualScorer` | 個人モードの全UI・state管理 |
| `SeriesListEditor` | 個人モードのシリーズ一覧の編集UI（採点画面とテンプレート管理画面で共用） |
| `SeriesCard` | 1シリーズ分の入力・内訳表示 |
| `TeamScorer` | 団体モードの全UI・state管理 |
| `JsonModal` | インポート/エクスポート（個人のみ） |
| `TemplateModal` | テンプレート管理画面（左に一覧、右に `SeriesListEditor` の編集欄） |
| `useFutureUnlock` | 十年後モードの解放（ジュニアモードを10回切り替えると表示。個人・団体で共有） |

### 9.1 テンプレート（個人モード）

よく使うシリーズ／演技構成を保存して呼び出す機能。保存先はブラウザの **localStorage**
（キー `mens-rg-scorer:templates:v1`、`src/scoring/templates.ts`）。

| 種類 | 中身 | 保存する場所 | 呼び出す場所 |
|------|------|-------------|-------------|
| シリーズ | 手具 + `Series` 1つ | 各シリーズの「テンプレートに保存」／管理画面の「新規」 | 実施減点の横のプルダウン（そのシリーズを置き換え）／管理画面の「採点画面に追加」 |
| 演技構成 | 手具 + `Series[]` | ツールバー・操作バーの「構成を保存」／管理画面の「現在の構成を保存」「新規」 | 管理画面の「採点画面に読み込む」（手具ごと差し替え・確認あり） |

どちらの種類も管理画面の「新規」から空のテンプレートを作って、その場で組み立てられる。

- テンプレートが持つのは**構成だけ**。`executionDeduction` は保存時に0にし、読み込んでも
  そのシリーズの実施減点は現在の値を残す（採点のたびに入れるものなので）
- 同じ名前で保存すると上書き（`upsert`）。一覧は更新日時の新しい順
- プルダウンは登録時の手具で「この手具」「他の手具」に分ける（`splitByApparatus`）。
  他の手具のテンプレートも読み込める。読み込むときは、今の手具で入力できない内容を落とす
  （`stripForApparatus`。下記「その手具では入力できない内容」）
- 管理画面はテンプレートを**カード**で並べる（名前・手具・シリーズ数・更新日と、
  `describeSeries()` による中身の要約「投げ→前宙→キャッチ」）。カードを選ぶと
  名前・手具・シリーズ内容を**採点画面と同じ `SeriesListEditor`／`SeriesCard`** で編集でき、
  変更はそのまま保存される（実施減点の行は出さない）
- レイアウトは画面幅で切り替える（`useNarrow()` ＝ `matchMedia("(max-width: 900px)")`）。
  広い画面は左カード・右編集欄の2ペイン、狭い画面はカードのみを出し、選んだら
  編集シート（`.tpl-sheet`）を重ねて「← 一覧」で戻る
- 壊れた保存データは `normalizeTemplateStore()` が項目単位で捨てる（読み込みで落ちない）。
  端末をまたぐ場合は管理画面の書き出し／読み込み（JSON）を使う
- **シリーズのタグ**（`SERIES_TAGS` / `seriesTags()` in `analysis.ts`）はシリーズの入力から自動判定する：
  投げ（投げ上げあり）／投げタン（`Unit.isThrowTumbling`）／三宙（`maxSaltoChain >= SALTO_CHAIN_TAG_MIN`＝3）／
  つなぎ（`hasConnect`）。採点画面の各シリーズの見出しと、テンプレートのカードに表示する
- **手具「共通」**（`COMMON_APPARATUS`）：どの手具でも使える内容だけを持つテンプレート。
  手具固有の要素（二つ投げ・左手投げ・手具を使った投げ／キャッチ・2つ同時キャッチ・ロープ跳び）が
  入っていると共通にできない（`commonBlockers()` が理由を返す）。編集時はそれらの入力欄を出さず
  （`SeriesCard` の `common` プロパティ）、採点は `scoringApparatus()`（＝スティック）で行う。
  シリーズのプルダウンでは共通を先頭に出し、構成テンプレートを読み込むときは手具を変えない
- **テンプレートの検索**（管理画面）：フリーワード（名前・手具・`describeSeries` の中身、空白区切りのAND）、
  タグ（選んだタグをすべて含む）、**難度と点数(D)の範囲**で絞り込む。難度・点数は
  `templateMetrics(series[], apparatus, junior)`（最高難度と、そのテンプレート単体で採点したときのD）で求め、
  カードにも「難度 C・D 0.3」として出す。絞り込みは構成テンプレートにも同じ条件で効く
  （構成はどれかのシリーズがタグを持てば一致、難度・点数は構成全体の値）
- 「構成をテンプレートに保存」は管理画面を開かずに実行できる。上部のツールバーと、
  画面下に貼り付く**操作バー**（`.action-bar`：合計点＋構成を保存＋テンプレート）の両方に置く。
  操作バーは `position: sticky; bottom` で、入力の途中でも上に戻らず保存・スコア確認ができる
  （狭い画面では内訳を隠して1行に収める）

#### その手具では入力できない内容（`apparatusBlockers` / `stripForApparatus` in `analysis.ts`）

手具を切り替えても、他の手具のテンプレート・JSON・共有URLから読み込んでも、シリーズの中身は
そのまま残る。入力画面はその手具で入力できるものしか出さないので、残った内容は
**画面に出ないまま採点に効いてしまう**（実際の演技データで、スティックに残った
「手具を使ったキャッチ」が技術加点＋0.1になっていた。ロープ跳びが残れば難度＋0.3）。

**投げている間の手具操作**（`handsEmptyFlags`）：投げてからキャッチするまでは手元に手具が
無いので、その間の技に手具操作は付けられない。手具が1つの種目（スティック・ロープ）では
投げている間ずっと、クラブ・リングでも二つ投げの間は同じ。技の最中の投げ（`isThrow`）は
開始時は手元にあるので、その技自体は手具操作あり。採点（`stripForApparatus`）でも
入力画面（`SeriesCard` は手具操作のチェックボックスを出さない）でも、自動生成
（`autoTemplate` が組み立てたシリーズを通す）でも同じ判定を使う。

対象は入力画面の出し分けと同じ：手具を使った投げ／キャッチ（`USE_APPARATUS_TAG`。
`APPARATUS_USE` がクラブ・リングだけ true）／2つ同時キャッチ／必須投げの種類
（`REQUIRED_THROW_OPTIONS`：スティックの左手投げ、クラブ・リングの二つ投げ）／
ロープ跳び（ロープだけ）。

- `computeScore` は採点の前に必ず落とす（落とすものが無ければ同じ配列を返すので、
  ランダム生成で何度呼んでも複製のコストは無い）
- 手具の切り替えは、落ちる内容を挙げて確認する（`IndividualScorer.changeApparatus`：
  「スティックでは入力できない内容（二つ投げ・2つ同時キャッチ・手具を使った投げ・キャッチ）が
  あります。外して切り替えますか？」。キャンセルすれば手具は変わらない）
- シリーズテンプレートの読み込み・追加、構成テンプレート、JSON／共有URLの読み込みは黙って落とす
- 手具固有の入力を増やしたら `apparatusBlockers` と `stripForApparatus` の両方に足す
  （共通テンプレートの `commonBlockers` も同じ考え方の一覧）

**同時に実施できない組み合わせは入力画面で出さない・選べない**（手具の出し分けとは別の制約）。

- **2つ同時キャッチは2つとも空中にあるときだけ**（`catchTwoFlags`）。クラブ・リングで、
  そのキャッチの時点で手元の手具が0になっている（＝二つ投げで両方を投げている、
  または2本続けて投げている）ときだけチェックボックスを出す。片方が手元にあるなら
  同時には受けられない。すでに付いている入力は外せるように表示は残す
- **二つ投げと手具を使った投げは排他**（`SeriesCard`）。両方を投げてしまうと押さえる手具が
  手元に無いので、一方がオンのときはもう一方を `disabled`（`.is-disabled`）にする。
  両方オンの古いデータは外せるように、オンのチェックボックスは無効化しない

手具に無関係な加点行は表示しない：二つ投げ4動作加点は `hasTwoThrow(apparatus)`（＝リング・クラブ）のとき、
様々な跳び加点は `apparatus === "rope"` のときだけ表示する（`ScoreSummary` の集計と `SeriesCard` の内訳の両方）。
値の計算自体は手具に関係なく行われるため、非表示でも 0 のまま `dScore` に含まれる。

トグルスイッチは `.switch` / `.switch-knob` / `.switch-row` / `.switch-label`（`src/index.css`）。
`role="switch"` + `aria-checked` を持つ `button` で実装する。

シリーズ入力（`.skill-row` / `.skill-block`）は広い画面では横並び、**720px以下では縦積み**にする
（`@media (max-width: 720px)`：ブロックを幅いっぱいにし、間の矢印 `.arrow` を下向きに回す）。
横幅の狭い端末で採点画面・テンプレート編集シートが横スクロールしないようにするため。

- スタイリングは **glassmorphism デザインシステム**（`src/index.css`）
- State 更新は `structuredClone` でイミュータブル
- `SaveData` 型（`{ version: 1, apparatus, junior, series, ... }`）でファイル/JSON保存

---

## 10. 未実装・要確認事項

- [ ] §3.8 個人選手モード（団体内個人、A=3.00満点の別採点方式）
- [x] 団体のE実施減点（各シリーズ／演技全体の手入力・合算）
- [x] 手具ごとの要求要素チェック（§3.2、`APPARATUS_REQUIRED_ELEMENTS` の手動チェック＋§3.5.6.3 減点）
- [x] §3.5.5.5(4)① 様々な跳び加点（ロープ）
- [x] §3.5.6.3 違反・欠如のA減点（開始/終了/音楽/徒手系群）
- [ ] §3.5.5.5(4)②③ 様々な跳び加点（跳びの形の多様性 / その場回転跳び2回転）— 跳びに形フラグの入力追加が必要
- [x] 手具別必須要素の自動判定化：スティックの右投げ右受け（`auto: "rightThrow"`）
- [ ] 手具別必須要素の自動判定化（ころがし・プロペラ回旋・まわし・転回系の投げ受けは現状手動チェック）
- [x] §3.5.6.4 芸術と多様性の欠点テーブル（主観評価の手入力・`ART_DEDUCTION_ITEMS`）
- [x] ジュニア適用規則（§10 変更規則1）の難度認定と投げ回数（→ §8.5）
- [ ] ジュニア適用規則 1-1〜1-2（使用手具を1つに限定するUI制約）
- [ ] 団体のA減点ロジックの精緻化（暫定 `missing.length * 0.3`）
